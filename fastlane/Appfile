package_name("com.valhalla.thor")

# DYNAMIC KEY PATH
# 1. Tries to use the Environment Variable (Great for CI or Custom paths)
# 2. Falls back to the relative path (Great for local defaults)
json_key_file(ENV["JSON_KEY_FILE"] || "../app/google-play-api.json")