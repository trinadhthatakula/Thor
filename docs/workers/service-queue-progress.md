# Thor service-queue progress

**Updated:** 9 September 2026. Times below are UTC.

**22 agreed milestones: 20 complete; T22 is incomplete and blocked after serial checks of all three emulators; T19 remains deferred.** Root startup 20+20 is independently verified, but its Apps list is not scrollable in the preserved configuration. Shizuku is not running and its idle-state preflight is ineligible. The configured DeviceOwner AVD has neither Thor nor Dhizuku installed and is unprovisioned. Physical five-pass jank remains blocked. **No test, review agent or profiler is running.** The implementation, logger/footer corrections and Guardians presentation are merged. Groot = Root, Rocket = Shizuku, Star-Lord = Dhizuku; portraits use the theme's **primary** color. The user's inset edit and supplied vector bytes are preserved. Fixture preparation, individual tests and recovery after the power failure are work within these two milestones, not additional milestones.

**Branch:** `chore/service-queue-emulator-acceptance`, from `dev` at `eb2345e1` · **Implementation PR:** [#453](https://github.com/trinadhthatakula/Thor/pull/453), **merged by the user** · **Version:** 1952.

**Release boundary:** the user reports Play approval of `SPECIAL_USE` on 8 September; the Console decision/artifact has not been independently inspected. Version 1953 was already uploaded manually to internal. The user deliberately restored source to 1952; the next ready update is **1954 only after pending work is finished and the update is ready**. Policy approval does not close runtime gates. No release or complete physical-device acceptance is claimed here.

Earlier publication, build and review checkpoints below describe their state at the time, not an open PR or a new execution of those gates.

## PR preparation — 9 September

Local `test lintFossDebug lintStoreRelease assembleFossDebug compileFossDebugAndroidTestKotlin` completed successfully with the recorded Corretto 21.0.12.1 exception: **196 tasks, 33 executed, 39 from cache, 124 up-to-date**, in 2m56s. Both unit-test tasks reused Gradle's build cache; their restored XML reports each contain **2,682 tests / 221 suites, zero failures/errors/skips**. Foss lint reports zero errors, 66 warnings and 15 hints; Store release lint reports zero errors, 53 warnings and 16 hints. Neither report contains `MissingTranslation` findings or `SyntheticAccessor` errors. Existing compiler/lint warnings remain disclosed.

This preparation run adds host verification to the physical-device and emulator evidence below. It did not rerun device tests or close the remaining T19/T22 acceptance checks. Version remains 1952.

## Active T22 — physical device first, then emulators

The user explicitly authorized T22 startup/jank profiling on the physical device and emulators, **starting with the physical device**. This supersedes the earlier emulator-only restriction for these profiling actions, not for unrelated destructive operations. T19 is deferred, not complete. No permission/broker/display/battery changes, app-data clearing or release-package intervention are included.

Initial read-only discovery identifies USB `1da5425f`, Xiaomi/POCO **25053PC47I / onyx**, Android 16 / API 36, boot complete and SELinux Enforcing. Thor Debug **1.95.2 / versionCode 1952** is installed with SHA-256 **`e0aa969829a4024ef262d40b90df0ef8354f9bd3f1a041e60ab873f5ff61b16e`**; it differs from the retained emulator APK and will be reported as its own build/device cohort. Release **1.95.3 / versionCode 1953** is also installed and will be left untouched. At initial discovery the debug process was absent, no active Thor service was visible, the screen was awake/unlocked, and POST_NOTIFICATIONS and Shizuku authorization were already granted. That checkpoint preceded the completed durable-state preflight and startup cohort below; it is not current process state. Initial charging battery level is 16–17%, about 39.6°C; current HAL CPU maximum is 54°C, GPU maximum 46.2°C and thermal status 0. Cached thermal entries are not current readings. The active display reports 1280×2772 at approximately 120 Hz; no setting was changed.

The bounded offline startup-protocol review **completed** (`physical-startup-preflight`, `wf_40d09a8a-514`); no agent/build will run alongside the actual performance cohort. The physical durable-state preflight **passed** (`physical-state-mn6safuc/record.json`): two identical DB/WAL captures, schema 9, integrity/FK checks, unchanged identity, zero DATA/SWEEP/output/freezer/WorkSpec rows, no active service. Private cache has no archive staging/bundle/read-copy or `share_work`; `files` has no partial-archive ledger/temp file. External cache is empty: `run-as` listing was denied, but ordinary ADB shell listing succeeded without root, grants or settings changes. Requested-permission classification found both declared dangerous permissions already held; undefined vendor/broker declarations are not grant candidates.

The installed APK exactly matches `app/build/intermediates/apk/foss/debug/app-foss-debug.apk`; all 21 DEX files were scanned and contain neither removed `ServiceQueueLatencyProbe` nor temporary `SweepRootVisibilityTest` identifiers. No embedded VCS record was found, so this establishes local artifact identity, not reproducible source attribution. It remains separate from the emulator APK. At the pre-pilot checkpoint battery was 21%, charging, thermal status 0; sampled CPU maximum was 58.6°C, GPU maximum 49°C and skin 43.675°C, with no override. The prospective design excluded pilots and one warmup per condition, followed by 20 process-cold and 20 same-PID Activity-recreated warm samples if eligible, with initial-display—not fully-usable—timing labels. Collection and verification are now complete as recorded below. Physical, emulator, debug/release and differing APK results will not be pooled.

**Physical pilots:** the initial excluded cold launch reported 1,516/1,520 ms TotalTime/WaitTime, but this ROM wrote no XML to `/dev/tty`; its observation check failed. The first warm pilot reported 314/330 ms with same-PID/new-Activity behavior, but the helper incorrectly required visible Apps/Freezer/Settings labels where the UI uses icon accessibility descriptions. Both incomplete pilot records remain preserved, not counted. A unique diagnostic XML file and the observed Home accessibility contract corrected the helper; ten lifecycle/parser assertions and two actual-capture Home checks passed offline. The corrected excluded pilots **passed**: cold **1,274/1,282 ms**, warm **251/256 ms**, normal unobstructed Home, lifecycle contract, quiet empty queues, same build/boot and unchanged grants (`physical-startup-pilot-excluded-qwq4n_kp/`, `physical-startup-pilot-excluded-aubrujqn/`). These are initial-display proxies, not a distribution or jank verdict. The next fixed cohort is cold then warm, one excluded warmup and 20 measured samples per condition, stopping on any failed check with no outlier removal/retries. Per-sample observation stays outside `am start -W`, and its inter-sample workload remains disclosed.

**Physical startup cohort — interrupted:** `physical-startup-batch-oqvd3cx_/record.json` records **20/20 completed cold samples**, an excluded warmup for each condition, **3 completed warm samples**, and failed warm attempt 4. The failed command reported WARM / 179 ms TotalTime / 191 ms WaitTime, but the subsequent Activity dump has `isSleeping=true`, display `OFF`, keyguard showing and Home `STOPPED`; it therefore fails the required resumed-Activity check. That timing is excluded. The complete 20+20 batch did not pass, no attempt was retried, and no timeout/lock setting was changed. A later read-only check found the device awake/unlocked again; this does not identify who or what changed its state. PID 11806 survives. A fresh post-stop snapshot (`physical-state-7eabv94x/record.json`) again passes schema/integrity/FK checks with all queue/output/freezer/WorkSpec aggregates empty and unchanged identity. Both offline reviewers **completed** (`physical-startup-results`, `wf_4c811ae6-875`). The interrupted run's separate cold TotalTime distribution is median **1,323 ms**, nearest-rank p95 **1,369 ms**, range **1,262–1,394 ms**; WaitTime median **1,328.5 ms**, p95 **1,376 ms**, range **1,268–1,397 ms**. All 204 referenced DB/WAL hashes match. The absence guard was executed but its raw PID probes were not in the command ledger; the repeat now logs those probes. Optional preference maps are empty because the two named files are absent—this is not proof of unchanged preference values generally. No device profiling ran during the review. Warm distribution and physical jank remain incomplete.

**User-requested repeat:** the user subsequently reported that the device had locked, said they would keep it unlocked, and explicitly requested the tests again. The fresh **full 20 cold + 20 warm** cohort **completed all 42 attempts: 40 measurements and two excluded warmups**, with no failed check (`physical-startup-batch-vop1q0h9/record.json`; command `kk2bycuce` finished). Raw PID probes are included in the ledger; the interrupted cohort is not pooled. No agent/build/profiler ran alongside it, and no lock/timeout setting changed. Independent raw-timing and preservation verification **completed** (`physical-repeat-verification`, `wf_bd87a597-718`, two reviewers, zero workflow errors). All 40 measurements satisfy the raw cold/warm lifecycle contract; warm PID 31369 survives with a new ActivityRecord each launch. All 84 state boundaries and 336 DB/WAL hashes passed; read-only SQL on disposable copies reproduced the empty aggregates and integrity/FK results without modifying original evidence. Identity/service helper internals have stored summaries, not a complete raw-command replay.

| Physical startup proxy (ms), n=20 each | Median | Nearest-rank p95 | Minimum | Maximum |
|---|---:|---:|---:|---:|
| Process-cold TotalTime | **1,278** | **1,321** | 1,217 | 1,351 |
| Process-cold WaitTime | 1,283.5 | 1,327 | 1,225 | 1,356 |
| Same-process Activity recreation TotalTime | **169.5** | **212** | 144 | 229 |
| Same-process Activity recreation WaitTime | 178.5 | 219 | 156 | 232 |

Median averages the middle two values; p95 is the 19th of 20. Warm first/last-ten TotalTime medians are 187/157 ms (WaitTime 197.5/165.5): drift is retained, not removed or treated as stationary. All observed boundaries were awake/unlocked with thermal status 0 and the same 14 grant entries. Display remained reported at approximately 120 Hz, battery 27–28% charging; sampled skin temperatures were 35.049–38.448°C and CPU 42.1–58.0°C, not continuous thermal monitoring. Both named optional preference files remained absent; this does not establish every effective preference value. These are debug-build initial-display proxies, not fully-usable latency, storage-cold launches, or a matched-baseline speedup/regression claim. **Physical startup collection and verification are complete; physical jank and emulator profiling remain open.**

**Physical jank pilot validation:** native `android.surfaceflinger.frametimeline` traces are processed locally with the private, SHA-256-pinned official Perfetto v58.2 host processor. No Android package is changed and no trace is uploaded. The first excluded Apps-scroll pilot (`physical-startup-jank-pilot-excluded-x32uggax/`) exited nonzero with a zero-byte trace; its exact numeric exit was not persisted, and its cause remains unestablished. An excluded three-second idle diagnostic then succeeded (116,548 bytes, same PID). Switching prospectively to native `--background-wait` and periodic service-side file writes produced a completed excluded up/reverse pilot (`physical-startup-jank-pilot-excluded-u_r1bji0/`), 213,063 bytes, with displacement, quiet-state and same-PID/Home restoration checks passing.

Local analysis identifies Thor PID 31369 and one HomeActivity layer with 199 actual / 199 expected surface-frame records, unique surface tokens and no unfinished actual slices. The trace's BOOTTIME snapshots align exactly with trace timestamps, matching the device uptime segment clock. However, Perfetto reports **`config_write_into_file_discard=1` (data_loss severity)** because streaming with a DISCARD buffer can lose data. This is a configuration warning, not proof which frames were lost; the pilot does **not** pass coverage acceptance. The next excluded pilot changed only buffer policy to the recommended RING_BUFFER and captured successfully (`physical-startup-jank-pilot-excluded-3c_cqtog/`, 267,865 bytes). Preliminary local analysis finds 243 actual / 243 expected records, exact unique surface-token pairing, complete slices inside the conservative uptime interval and no reported error/data_loss stats. The focused offline verifier **completed** and accepted this collector for a qualified observed-surface distribution. Global service `chunks_discarded=3` is constant across all four snapshots, including packets bracketing the segment; it is distinct from buffer discard/overwrite/wrap, all zero. Flush requested/succeeded 6/5 comes from a mid-flush snapshot, not a demonstrated failure. Service start → disable is **30.003660197 s**, followed by the final read-completion packet: the shorter 21.931 s `trace_bounds` span is not premature termination. Final-flush success remains unestablished because a flushed marker also permits error/timeout completion and no final-outcome snapshot follows it. Parent re-execution agrees: 243 paired records, 4 App Deadline Missed and 13 Buffer Stuffing classifications; this pilot stays excluded. Twelve offline parser/pairing/percentile checks and three actual-trace negative mutations passed. Official [counter definitions](https://github.com/google/perfetto/blob/8665a1fa48ac395bedbffa543e2330354cf717eb/protos/perfetto/common/trace_stats.proto) and [lifecycle guarantees](https://github.com/google/perfetto/blob/8665a1fa48ac395bedbffa543e2330354cf717eb/protos/perfetto/trace/perfetto/tracing_service_event.proto) explain the interpretation, not exact vendor-source provenance.

**Prospective physical measurement cohort:** five fixed Apps-list up/reverse passes, one 600 ms swipe each way within observed list bounds; no operation, restart or configuration change. Use the same existing PID, exact APK/boot, quiet-state/grant boundaries and normal Home restoration. Each trace must pass BOOTTIME/segment containment, unique one-layer actual/expected pairing, no reported error/data_loss, stable global discard snapshots bracketing the segment, zero buffer-loss counters, and bounded service start/disable/final-read checks. Every attempt is retained; stop on the first capture/control/trace check failure, with no outlier removal or within-cohort retry. Collector/validator hashes are frozen in the batch record. Analyze only after each tracer ends; no review/build/other profiler runs alongside capture. These are consistency checks on observed records, not independent proof of lossless producer emission or a whole-app smoothness threshold. This non-operation Apps-list cohort does not cover populated Guardians detail or operation-loaded frames; accessibility observation overhead remains. Earlier failed emulator HWUI/reset cohorts stay separate and failed.

**First physical five-pass attempt stopped in validation, not capture:** `physical-jank-batch-693hwl0l/record.json` contains one completed capture (`physical-startup-jank-measurement-6dikukie/`) but no completed batch. The validator incorrectly required ordinary `trace_bounds` to begin before the pre-input marker; the first ordinary frame actually begins about 82 ms afterward. Service lifetime correctly brackets the segment and ends after 30.004278634 s with final read completion. This was a validator defect, not app jank or demonstrated trace loss. The predicate now uses service lifecycle bracketing only, retaining frame containment and all other validity checks. Offline re-analysis passes both the 243-record pilot and this 249-record capture, with exact expected pairing and no outside-segment target records. The interrupted attempt remains excluded; the next five-pass run is a new cohort with the corrected validator hash, unchanged collector/workload, no within-cohort retry or outlier removal.

**Corrected physical cohort — stopped on a real validity guard:** `physical-jank-batch-zn6zze95/record.json` also stops after its first completed capture (`physical-startup-jank-measurement-s272fyye/`). Global service discarded chunks increase **4 → 5** between the pre-segment and post-segment stats packets; later snapshots remain 5. This is not buffer overflow (buffer discard/overwrite/wrap counters are zero) and no parser/data_loss severity is reported, but the trace cannot attribute the discarded chunk to a producer or place it precisely inside/outside Thor's segment. The prospectively frozen stability guard therefore fails; it is not relaxed after seeing the result. This capture contains 264 target actual records, not an accepted complete distribution. Native ending and capture-level preservation passed. **Zero complete physical five-pass cohorts; no third attempt is started.** Physical startup is complete, physical observed-surface jank batching is blocked, and populated detail/operation-loaded frames remain unmeasured. Next action is read-only emulator/resource discovery; earlier failed physical/emulator cohorts stay excluded and separate.

### Current emulator preflight and physical stop

**Expanded unattended authorization:** the user requested checks on **all three** emulators—Root, Resizable Shizuku, and Thor DeviceOwner API37—while they are away, prioritizing T22 and then attempting T19 if feasible. Fresh discovery confirms Root is the sole running emulator at `emulator-5554`; the physical device is no longer connected. Exact additional AVD name is **`Thor_DeviceOwner_API37`** (display name `Thor DeviceOwner API37`); its existing image is API 37.2 Google Play 16 KiB / arm64, configured with 2 GiB RAM. Resizable remains the existing 2 GiB Google APIs image; Root is 4 GiB. `Row10_API30` remains out of scope. Measurements will be serial, without competing reviews/builds/profilers. The offline three-reader readiness workflow (`emulator-validation-readiness`, `wf_2deeeb41-5b1`) **completed**, with all three results consumed and no workflow errors. Existing helpers need narrow emulator identity/snapshot adapters; quiet WorkManager must exclude ENQUEUED/BLOCKED as well as RUNNING, and full retained records plus startup-cleanup eligibility need preservation checks. Dhizuku selected cache clearing is Root-gated, not a positive Dhizuku acceptance scenario; other privileged operations require actual capability and fixture postconditions. No new performance measurement or T19 operation has started. Preserve the same 22 milestones, consent boundaries and owned-fixture-only mutation scope; record blockers rather than altering settings/grants or deleting outputs.

Fresh discovery listed only the physical device; the 24 GiB host reported 84% memory free. Only **Thor_Root_API36** was started using its existing configuration and `-no-snapshot`, without wiping, restoring a snapshot, or changing AVD settings. Fresh identity is **emulator-5554**, API 36, SELinux Enforcing, boot `7a6becdb-cc55-4385-8745-d6e66c6ce10b`, fingerprint `google/sdk_gphone64_arm64/emu64a:16/BE2A.250530.026.F3/13894323:userdebug/dev-keys`. Thor Debug is installed and its process is absent; **no Thor launch has occurred**. The subsequent read-only package check matches the full retained probe-free APK SHA-256 **`67f7d981b237465a06b78dde5309001dbcf830a654664713fc98d57b006a70c2`**, version 1952, with 13 grant entries including notifications granted, no active Thor service, awake/unlocked screen and thermal status 0. A debug process **PID 3074** appeared without an assistant-issued Thor launch; Nexus Launcher remains top-resumed and no Thor HomeActivity exists. Its start cause is not established. Boot identity is unchanged. This is not a warm Activity sample or proof of unchanged startup side effects. Durable-state/startup eligibility remains pending. Resizable_Experimental and Thor_DeviceOwner_API37 have not been started.

**Root read-only eligibility checkpoint:** `queue-snapshot-1788920498888220000.json` records two identical DB/WAL captures, schema 9 and passing integrity/FK checks: **3 SUCCEEDED sweeps, zero DATA tasks, zero READY handoffs, zero WorkSpecs and no queue service**. Independent SQL on a disposable copy confirms zero freezer rows and no sweep retention due within the next hour (earliest deadline `1788965863391`; sampled device time `1788920497` seconds). The captured DB/WAL hashes match retained Root evidence; that is stronger than aggregate-count equality but is still a quiet snapshot, not an atomic cross-database backup. Both owned fixture sentinels retain SHA-256 `8e8c5d3b83f31a624215dc5a8d1f1ed15fb6dec0e1f78f46e0000adcce5e93fb`; both cache directories remain empty as intended. No fixture Activity was launched. External debug cache is empty; private inventory shows no archive/share staging or orphan ledger, and the only observed preference file is the app-label cache. User 0, awake/unlocked, thermal status 0, notifications already granted, and **7,978,528 KiB** available on `/data`. PID 3074 remains background-only with Nexus Launcher foreground. No measured startup or UI input has occurred at this checkpoint.

**Private harness work:** sanitized parent observations, including exact fixture APK/sentinel hashes and the bounded private-file inventory, are available to the harness in ignored `runtime-20260908/emulator-parent-preflight.json`. These are Root observations only, not permission to assume the same files or grants on the other AVDs. `emulator-startup-adapter` (`wf_3ccbcffe-bdc`) and its resumed author were **stopped** after the author reported overlapping edits to the two private helper/test files. A factual message to the workflow author had started a resumed agent; both runs were explicitly stopped before any runtime use. Saved files are preserved, not overwritten. The single **read-only** handoff review `emulator-harness-handoff` (`wf_50236891-f13`) **completed**. It found two private-helper blockers: observed metadata/lock files were rejected, and missing fixture sentinels could become an empty baseline. Parent reproduced the failing test, completed those narrow fixes, and added regression coverage for the later exhaustive inventory (including the nested WorkManager lock and empty `app_dxmaker_cache`). Unknown files/subdirectories, symlinks, staging and outputs still fail closed; no on-device content was removed. **20 offline tests pass**. Two read-only preflights stopped on observed inventory mismatches before any lifecycle command; the final preflight (`emulator-startup-preflight-6tv53ocd/record.json`) **passes**. This is a harness-coordination interruption, not an app/emulator failure. No device commands or production/test-source edits were delegated. The adapter must pin emulator/boot/APK, preserve complete retained queue records and existing fixtures/grants/preferences, and keep the fixed excluded-pilot then 20-cold/20-warm protocol. Actual timing waits until review finishes; no competing build/review/profiler will run during measurement.

**Root excluded startup pilots passed:** `emulator-startup-pilot-7lbb4a1s/record.json` records cold **1,040/1,042 ms** TotalTime/WaitTime and warm **123/124 ms**. Cold has proved process absence; warm retains PID 5417 and changes the resumed ActivityRecord. Both observe unobstructed normal Home and pass full retained-record, fixture, grant, URI-permission, display and preference preservation. The 480-command ledger includes raw PID probes; observations span about 18.4 seconds outside/around the two timed starts. These two pilots are excluded, not a distribution or jank pass. Collector SHA-256 is `b25b44d6df58a1497a8da6a0fddb702a97523915cd8f935da2a059c76edbe07b`. Next is the fixed cohort: cold warmup + 20, then warm warmup + 20, first-failure stop, no retries or outlier removal, no competing agents/builds/profilers.

**Root fixed startup cohort — collected:** `emulator-startup-batch-5_p31bt8/record.json` completed **42/42 attempts: 20 cold + 20 warm measurements and two excluded warmups**, exit 0, unchanged collector hash, no failed check/retry/outlier removal. The command ledger has 8,140 entries over about 352.1 seconds. TotalTime median / nearest-rank p95 / range: **cold 1,037.5 / 1,060 / 1,006–1,065 ms**; **warm 87.5 / 117 / 70–125 ms**. WaitTime: cold **1,039.5 / 1,061 / 1,008–1,068 ms**; warm **88.5 / 118 / 71–126 ms**. These are separate Root-emulator initial-display proxies, not app-ready timing, frame smoothness or a physical-device comparison. The workflow `root-startup-and-frames` (`wf_49f5ccd4-cad`) **completed**, and all results were consumed. Independent verification reconciled all 8,140 commands, 42 launches, 21 force-stops, 85 boundaries and 340 original DB/WAL files; the excluded pilots were verified separately. All 21 warm launches retain PID **17704** and replace the ActivityRecord. Fourteen scoped full-row fingerprints, fixtures, grants, preferences and protected files remain invariant. Original evidence is unchanged. WorkManager DB/WAL bytes vary while WorkSpec records remain unchanged; this is not byte-for-byte whole-database preservation. **Warm drift is retained:** first/last-ten TotalTime medians **98 / 80.5 ms**, WaitTime **99 / 81 ms**; do not present these as stationary estimates.

**Root frame harness — reviewed and corrected, not yet measured:** the private adapter preserves the fixed 30-second RING_BUFFER configuration and unchanged analyzer. The completed review and resumed read-only reviewer found three concrete gaps: Home could satisfy the generic list selector, existing-tracer refusal happened after navigation, and an ambiguous tap failure skipped restoration. Parent reproduced all three as failing offline tests and corrected the narrow control flow. Apps now requires the observed App List header and Search apps field above the scroll area; saved normal Home is rejected. Existing tracers block navigation, and potentially dispatched taps enter guarded restoration. **7 frame tests and 20 startup tests pass**; existing helper hashes match their pre-adapter pins. No agents, builds or other profilers may overlap the upcoming excluded Root frame pilot or fixed five-pass cohort. Root frame coverage and the other two emulator cohorts remain pending.

**Root excluded frame pilot — blocked before recording:** `emulator-frame-pilot-kmg6uqy8/record.json` stops with `No unique Apps scrollable`. The observed Apps page shows the short existing list (including both owned fixtures), with **no scrollable node** at the unchanged display/filter state. No swipe or Perfetto process was started. The guarded return to normal Home and full post-boundary preservation succeed with PID **17704** unchanged. No retry, fixture installation, filter/display change or coverage-policy relaxation was used; a five-pass Root scrolling cohort is not eligible in this state. Existing startup evidence remains valid. The IDE build hook could not validate the private Python files (limited build diagnostics); the executable offline Python suites are the validation evidence, not an Android build claim. Root was then stopped normally through its freshly identity-checked emulator console; no wipe or app force-stop was issued.

**Resizable Shizuku preflight — blocked:** the existing AVD was started alone, using its unchanged configuration. Its emulator log reports failed `default_boot` snapshot loading followed by a normal cold boot; it also reports its own automatic timeout/log-buffer initialization. No timeout, display, broker or permission command was issued by this session. Fresh boot `b98673fa-9d96-473c-aae0-b5dd3b379783`, user 0, API 37 and the same debug APK hash are verified. Thor is already running as PID **3139** while Launcher is foreground; no `am start`, force-stop or Thor navigation was requested. The full preflight (`emulator-startup-preflight-s0uvorfx`) stops at the display classifier: two built-in descriptors contain `FLAG_ALLOWED_TO_BE_DEFAULT_DISPLAY`, one ON and one OFF. This is a **harness default-display identification gap**, not proof that the active display is off.

A separate **read-only diagnostic**, `emulator-eligibility-3s0g9i5r/record.json`, preserves raw probes and stable DB/WAL copies without bypassing the measurement gate. It finds Shizuku Manager installed but **no Shizuku server process**, no queue service, 30 successful DATA rows, **24 successful sweeps** and **one freezer entry**. The idle-state validator rejects that freezer entry; it was not removed. The earlier 27-sweep history is not preserved across boot (the three-row reduction is consistent with the previously expired rows, but deletion cause is not independently attributed). Free space is **3,606,816 KiB**, still below the unchanged 4 GiB XAPK guard. No startup/frame cohort or T19 operation was attempted. Current broker consent/effective privilege remains unverified; no repair was attempted. Resizable was then stopped through its identity-checked emulator console before starting DeviceOwner.

**DeviceOwner Dhizuku preflight — setup mismatch:** the exact configured AVD `Thor_DeviceOwner_API37` was started alone with no configuration overrides. Its emulator also reports failed `default_boot` loading followed by a cold boot. Fresh boot `53b9bf06-b7c1-4dda-8665-01595ec6bf2e`, user 0 and API 37 are verified. User 0 is the only user; **259 installed packages, zero third-party packages**, neither debug nor release Thor, and no Dhizuku/Shizuku package or process. `dpm list-owners` returns empty; independently, device-policy diagnostics report **provisioningState 0 / Device Owner Type -1**. The combination establishes that the expected testing setup is not present; the empty owner-list output alone is not treated as proof. No apps, owner, fixtures or grants were provisioned. Evidence: ignored `deviceowner-preflight-20260909.json`. DeviceOwner remains open at Launcher for inspection; Root and Resizable are stopped. No startup/frame measurement or T19 operation was submitted on this AVD.

**Unattended stop boundary:** all three authorized AVDs have been checked in sequence, not all three accepted. Further work needs an agreed scrollable Root workload, restored/verified Shizuku readiness with the existing freezer/history constraints addressed, and the intended provisioned Dhizuku environment identified or explicitly set up. Do not alter those conditions, delete history/outputs, or weaken coverage guards merely to close a milestone. T22 stays incomplete; T19 cancellation/recovery/conflict/overlap checks stay pending. No new commit, push, release, APK install, fixture/cache reseed, permission grant or physical-device action occurred.

The user then said they would disconnect the physical device and asked for its results. They were told it is no longer needed for current work: no physical test is running, startup 20+20 is verified, and five-pass scrolling jank is inconclusive rather than passed. **Physical actions are stopped; do not issue further physical-device commands under the current emulator phase.** Release 1953 and device settings/grants were not changed.

## Earlier emulator boundary — 9 September

**Subsequent host-resource stop:** the harness reported that background command `b9rcpy5ae` (“Restart the same emulator without wiping data”) was killed because the host was low on memory. Its saved log identifies `Resizable_Experimental` / emulator 37.2.7.0. A fresh read-only `adb devices` check then listed **no emulators**; this establishes that neither authorized target is currently reachable, not that both were killed for the same reason. This was an earlier long-running emulator command, not a restart initiated during the recent read-only review/preflight. No automatic restart, wipe, snapshot restoration or configuration change was attempted. Device validation remains paused; all process/queue observations in the following paragraphs are the last successful pre-stop boundary, not current device state.

Both frame follow-up reviews are complete; no review agent or device operation is currently running. A read-only functional preflight then found **Shizuku Thor PID 32688 no longer running**, with the same emulator boot and unchanged installed app/test hashes. Android's exit metadata records **04:18:55.227, `USER REQUESTED / REMOVE TASK`, status 0** for that PID. This is the platform's classification, not attribution to a particular person and not evidence of an app crash. No launch, force-stop or task removal was performed by this preflight. Root remains PID **9610**. A CRLF parsing error initially refused the AVD-name check; corrected parsing and a fresh emulator-only identity lookup confirmed the same two authorized AVDs, not a port/identity change.

Fresh stable snapshots remain quiet and match the previously projected queue state: Shizuku **30 successful DATA tasks, 26 successful / 1 partial sweep requests**; Root **3 successful sweeps, no DATA tasks**; both have zero READY handoffs and no unfinished WorkSpec. A fresh read-only retention query now finds **3 of Shizuku's 27 sweep requests already expired**, while all 30 DATA rows remain unexpired. Startup invokes retention pruning, so reopening is not eligible as an unchanged-history step. **No app restart, export submission, cancellation, pruning or output deletion was attempted.** The lower-footprint APK candidate therefore remains unexecuted, and the former same-PID measurement session cannot be resumed.

Evidence: `post-review-boundary-1788909859322.json`, snapshots `queue-snapshot-1788909779749584000.json`, `queue-snapshot-1788909780543889000.json`, and fresh retention snapshot `queue-snapshot-1788909859624798000.json`. Current free space is **3,516,628 KiB Shizuku / 7,974,640 KiB Root**; the 4 GiB XAPK guard stays unchanged. These fresh checks establish APK identities and projected queue state, not a new complete grants/preferences/fixture comparison. **At that earlier checkpoint: 20/22 milestones complete; T19 was active but paused, T22 had not begun. The current status is the opening summary and definite remaining list below.**

## Definite remaining list

| Milestone | Status | What remains | Evidence required to close |
|---|---|---|---|
| **T19 — Shizuku emulator acceptance and latency** | **Deferred — T22 takes priority** | Real fixture freeze/unfreeze, selected Fix Store and APK publication have verified postconditions. Export first-reopening is corrected and emulator-verified; both 20-sample warm batches are collected. Root selected-cache removal now passes after the SWEEP mount-namespace correction. Complete cancellation/reopening/recovery/conflict and queue-overlap checks. Temporary probes are removed; fresh host/APK/lint validation and all 22 retained presentation tests pass. Both emulators now have the verified probe-free app/test pair, and Root's final-source selected-cache check passes. Complete the remaining real-operation checks without hooks; Shizuku's large-XAPK pilot is still below its 4 GiB storage guard. The latest serial preflight also finds its broker inactive and idle-state eligibility blocked; DeviceOwner lacks the expected software/provisioning. No new T19 operation was submitted during this profiling phase. | Preserved Manager authorization, scenario postconditions and raw distributions; honest baseline comparison; final APK without hooks. |
| **T22 — Startup and jank check** | **Blocked — serial emulator checks finished; incomplete** | Physical startup **20 cold + 20 warm collected and independently verified**. Both physical five-pass attempts stopped; the latest hit an unattributed service-global discard increase. No third attempt; the physical device is disconnected. Root full preflight and excluded pilots pass; its **20 cold + 20 warm cohort is independently verified**, with warm drift disclosed. Resizable's fresh preflight is blocked: default-display classifier ambiguity, a retained freezer entry and no running Shizuku server. DeviceOwner is blocked before testing because Thor/Dhizuku/fixtures and provisioned ownership are absent. Populated-detail and operation-loaded frames remain pending. Keep separate device/build cohorts. Both earlier emulator five-pass HWUI attempts stopped; complete emulator frame coverage remains unestablished. The private no-reset Root adapter is reviewed and offline-tested, but its excluded pilot stops before swipes/tracing because the existing Apps list is not scrollable. Home and protected state are restored; no five-pass Root cohort is eligible without a separately agreed workload change. Shizuku's earlier same-PID session is gone; after the new boot its sweep count is 24 rather than 27, so cross-boot history preservation is not established. Matched-baseline comparison remains unestablished. | The historical 26-test presentation pass included four telemetry-only tests now removed. All 22 retained presentation tests now pass on the probe-free target with the corrected test APK, but use controlled Compose state. Real-operation frame measurements are still required; presentation tests and host gates do not establish smoothness. |

### 9 September temporary telemetry cleanup — host and presentation gates passed

Temporary latency hooks and their probe-only test assertions have been removed from 13 source/test files; the complete probe source is preserved privately as `ServiceQueueLatencyProbe.preserved.kt` (SHA-256 `68b9a1809447930d86c01b680a05e5a84bc710bb6fed059be38243710274b492`). Parent verification confirmed the preservation hash, ignored status, clean `git diff --check`, empty staging area and unchanged version 1952. Independent bounded diff review found no concrete defects: the seven non-UI production files match HEAD after only the enumerated telemetry removals; durable acceptance, target-claimed/cancellation/lease/ownership, notification/progress behavior, sheet-owned disposal and real NavDisplay regressions, and SWEEP mount-master/timeout regression remain intact. This is static review, separate from the executed tests below. Removed tests are four marker/draw-selection Android tests and one marker-only JVM tombstone test, not the lifecycle, cancellation or Root factory regressions.

The targeted IDE build tool timed out without returning a result. The corresponding daemon's latest build log reports `BUILD SUCCESSFUL in 1m 8s` and returns to idle, retaining the known Koin/D8 warnings; a separate forced CLI run will provide the definitive full-test/assembly record. The separate forced CLI run **passed in 4m8s, exit 0, all 234/234 tasks executed**: Foss and Store each **2,682 tests / 221 suites, zero failures/errors/skips**, with fresh XML checked and preserved. The one-test-per-flavor reduction is the removed telemetry-only tombstone assertion. Foss debug/test and Store release assemblies passed in that same run (`probe-free-host-1788895520800044000/`). APK verification/preservation also passed (`probe-free-apks-1788895819582986000/record.json`): debug `67f7d981…`, test `87633735…`, Store release `663e9fbb…`; package/version, signing verification, runner/target and all DEX entries were checked. Removed telemetry identifiers and the temporary Root diagnostic are absent from those DEX files. Both app variants remain 1952 / 1.95.2; local Corretto21 and the known Koin compatibility warning remain disclosed. Fresh sequential post-cleanup lint **passed with zero errors**, exit 0 for both: Foss **66 warnings/15 hints**, 70/70 tasks, 2m32s; Store **53 warnings/15 hints**, 51/51 tasks, 1m53s. Actual fresh XML/log evidence is preserved under `root-lint-1788895893469105000/`. The read-only Shizuku install preflight also passed (`probe-free-shizuku-install-1788896174341266000/`): approved AVD reidentified, quiet queue, both old APKs preserved, compatible signatures/ABI, identical normalized target manifest, unchanged-version candidate, fixture identity/cache/sentinel and grants recorded. Data-preserving Shizuku app/test replacement **passed** (`probe-free-shizuku-install-1788896206076118000/record.json`): target `67f7d981…`, test `87633735…`, normal Home launch, unchanged grants/boot/fixture APKs/cache metadata and hashes/data sentinels, unchanged fixture queue rows/counts/WorkSpec records, and quiet post-install snapshot. Only this AVD was updated; the Root emulator still has its earlier telemetry-bearing target/test APK. The first retained presentation run stopped with **QueueScreen 7/7 passed; TaskDetail 13/14 passed, one failure; QueueNavigationButton not run** (`probe-free-ui-tests-1788896347687191000/`). `systemBackDismissesUiWithoutDispatchingCancellation` failed at `Espresso.pressBack()` with `RootViewWithoutFocusException`: the Activity root did not obtain focus within ten seconds, before Back delivery or its dismissal assertions. This is retained failure evidence, not yet classified as environment versus regression. The device was awake; post-run focus belonged to Shizuku Manager, which activity events also show returning between test-Activity lifetimes. That is not proof of focus theft during the failing assertion. An unchanged **isolated Back-test diagnostic passed 1/1**, with live read-only focus samples and unchanged queues/grants/fixtures (`probe-free-back-diagnostic-1788896786154554000/`). The original timeout's cause remains unestablished. The one complete rerun **reproduced the same failure**: QueueScreen 7/7; TaskDetail 13/14 with Back again failing at test 14; QueueNavigationButton not run. Its postchecks passed unchanged queue records/grants/fixtures and a quiet boundary (`probe-free-ui-tests-1788896858247824000/`). The functional device gate is therefore still failing, not a completed pass or a dismissed flake. A focused full-class diagnostic reproduced 13/14 again with all preservation checks passing (`probe-free-class-diagnostic-1788897137090142000/`). Its 36 live focus samples show the current Thor **APPLICATION dialog window holding focus throughout the final ten-second wait**, alongside the visible, unfocused BASE_APPLICATION Activity selected by Espresso. Shizuku Manager was not the focus owner during that failing interval. Cached Espresso 3.7.0 source confirms the mechanism: default matching can select the Activity before dialog focus arrives; `RootViewPicker` then polls that same chosen root without reselecting for ten seconds. The narrow test-only correction adds `inRoot(isDialog())` to Espresso's original `onView(isRoot()).perform(pressBack())` path, retaining the real Back action and both exact dismissal/no-cancellation assertions. No production behavior, grant, setting or foreground-app state was changed. The test-only correction subsequently passed targeted IDE compilation (known D8/Koin duplicate-hint warning retained).

**Corrected-test GREEN:** forced `assembleFossDebugAndroidTest` plus `lintFossDebug` passed, exit 0, **109/109 tasks executed in 2m30s** (`dialog-test-host-1788898262077874000/record.json`). Fresh lint XML has **zero errors, 66 warnings and 15 hints**. The preserved test APK SHA-256 is `04bb08138ef3540c7661b9cd59c32c9af3a366f593f1e037d72761776ed4bfcd`; its debug certificate, package/runner/target and absence of removed probe/diagnostic identifiers were verified. A guarded **test-APK-only** replacement on `Resizable_Experimental` preserved the prior `87633735…` test APK and passed unchanged target `67f7d981…`, grants/fixtures, queue records and quiet-boundary checks (`dialog-test-install-1788898585387486000/record.json`). No app replacement or permission change occurred in this step.

The complete retained presentation run then passed **22/22: QueueScreen 7, TaskDetail 14, QueueNavigationButton 1**, with zero negative instrumentation statuses and unchanged grants/fixture controls/queue records, ending quiet (`probe-free-ui-tests-1788898618129736000/record.json`). This includes real Back delivery in the formerly failing full class, not merely the earlier isolated pass. All three prior failed full-class records remain preserved; their diagnosis is now resolved by the test-root correction, not a production workaround. Full JVM/Store gates above predate only this Android-test selector edit; Foss lint is fresh after it. Root still has its older telemetry-bearing APKs. A normal post-test Home launch and Privilege Check show **Shizuku — Granted**, without a grant/settings change (`ui-1788898968924298000.json`, `ui-1788898984837818000.json`); the status panel was then dismissed normally. Final tracked diff check is clean, staging is empty and version remains 1952. Active-operation runtime gates and T22 measurements remain open: **T19 active, T22 pending, 20/22 complete**.

### 9 September longer real export pilot — Background observed; active timing qualified

Only the existing disposable large fixture was replaced on `Resizable_Experimental` with a separately built **1,024 MiB stored-payload APK**: `9eef8c9fdd01215ecf8ca5549f552041f0491e8cb830b0d64319fded69998188`, **1,073,754,587 bytes**. Its manifest, version 1/1.0, certificate and Activity DEX are unchanged; no new permission, OBB payload or private-data access was introduced. Both fixtures' original APKs and complete authored files/cache were preserved with hash-verified tar baselines. Replacement changed only the large APK: small fixture, both sentinels/cache contents and metadata, app/test identities, grants and queue records were unchanged, and storage headroom passed (`large-pilot-build-1788899399244911000/`, `large-pilot-install-1788899540196711000/`). Root's fixture remains the older 128 MiB build. Old helpers pinned to `8b50d8ae…` must not be reused on Shizuku as if that were its current large fixture.

The separately excluded real XAPK pilot **passed**, task `d535925f-ad84-4c68-95f1-7e5be1a20929`, **9,021 ms**, attempt 1, parent/child success with claims/session cleared (`xapk-pilot-1788899895316383000/record.json`). Its first UI capture shows Running and Background. Background was actually tapped and the returned app sheet was captured with PID 32688 unchanged. The originally reported **+8,097.6 ms / 923.4 ms before terminal** margin subtracts host wall-clock observations from device epoch timestamps without clock calibration. It is retained as a historical estimate, **not proof that the capture completed while work was active**; the former active-Background timing conclusion is withdrawn. The device's own claimed-to-terminal interval remains 9,021 ms, not a measurement of uninterrupted copying. All eight checks pass, including unchanged prior/opposite queue records, grants/fixture controls, quiet completion, MediaStore publication and ZIP integrity. The **1,073,935,342-byte** public XAPK contains one exact-hash fixture APK and is preserved locally. No other work was cancelled or deleted. This is one functional-duration pilot, not a latency distribution or matched-baseline claim. **Active same-UUID reopening, cancellation isolation, overlap/conflict and recovery remain pending.**

A subsequent **probe-free completed-result recheck passed** without creating another export: after normally closing completed task `426a45b6…`, the first reopened large-fixture export form remained visible **31,937.0 ms** later, with no replayed Exported/Succeeded result, the same PID 32688, unchanged queue records and a quiet boundary. All six checks passed (`export-reopen-final-source-1788904553635774000.json`, UI `ui-1788904521698760000.json` → `ui-1788904553635774000.json`). This is current-source completed-result non-replay evidence, **not** active-task reattachment, a retry or a latency/frame sample.

### 9 September real notification reopening — identity passed, active timing not accepted

One bounded notification attempt used the actual SystemUI **Data operation in progress** notification after Background, not a synthesized task Intent. Task `426a45b6-319c-4515-8d03-ee0d18711803` succeeded in **8,628 ms**, attempt 1, with a correctly published **1,073,935,342-byte** XAPK. All eight publication/integrity/preservation checks passed and the observed new `thor://data-task/<UUID>/view` route matched that sole new durable task (`xapk-pilot-1788901369485696000/record.json`). The original helper reported Background at +3,056.5 ms, notification tap at +6,736.1 ms and capture at +9,642.0 ms: nominally 1,891.9 ms before and 1,014.0 ms after terminal, respectively. **Those are uncalibrated host/device-clock differences, not established before/after bounds.** Tap times are command-return times, not input-delivery times, and UI record timing includes helper work beyond the hierarchy dump. Its Running/Publishing labels do not establish an active capture; the original before-terminal assertion failed and the stronger calibrated assertion was never measured. The new cached notification route matching the sole new task corroborates identity, but does not independently bind the tapped SystemUI entry to that PendingIntent. Result: **9/10 checks passed; active same-UUID reopening remains pending**, not a production-regression finding. The failed timing record is preserved separately; no retry-until-green, accepted-work cancellation, output removal or permission/settings change occurred. The emulator ended quiet with unchanged fixture/grant/prior queue controls. This attempt is excluded from latency and frame cohorts. Read-only inspection confirms the UI helper stamps its record after the accessibility dump returns, so the timestamp cannot be moved backward to accept the visible Running labels. Post-attempt `/data` headroom is **3,600,412 KiB**, below this pilot's existing **4 GiB** submission guard: no further 1 GiB XAPK export will be submitted under that guard, and retained outputs will not be deleted to manufacture headroom. A fresh tracked-scope check remains clean (`git diff --check`), staging empty, 18 changed paths and version 1952.

### 9 September observation and remaining-gate review

A read-only screenshot pilot first failed PNG validation because Android placed a multi-display warning before the PNG bytes. The helper now resolves HWC display 0 / `EMU_display_0`, verifies its `DisplayDeviceInfo` is the active default, and explicitly selects that physical display. The next idle capture succeeded in **141.432 ms**, bounded by **161 ms on the device clock**, with unchanged boot/PID 32688 (`timed-ui-1788905595274876000.json` and PNG). The image was inspected and shows the existing unsubmitted large-fixture `.apk` export form. This is one idle capture—not a loaded capture guarantee, active-task proof, or frame-performance sample. No app/test source, permission or device configuration changed.

The six-agent feasibility/adversarial review completed (`remaining-emulator-gates`, `wf_c1a0f250-d06`). Its accepted corrections are applied above: historical cross-clock margins are estimates; claimed task intervals do not prove actual concurrent copying/privilege execution; screenshots need separate task identity and device-clock bounds. A single APK export is only an unmeasured lower-footprint candidate, not authorization to relax the XAPK guard. Share READY/expiry is not a quiet or artifact-preserving shortcut. Before any startup cohort, check retention deadlines, unfinished WorkSpec state and startup permission/reconciliation no-ops. Begin with one excluded idle measurement pilot, collecting dialog frame data **before** dismissing the dialog; proceed to batches only if preservation and measurement coverage pass. No new operation or performance cohort has been submitted in this review step. **T19 active, T22 pending; 20/22 complete.**

The two excluded **existing-PID idle frame pilots** then passed all preservation checks (same PID 32688/boot, exact app/test identities, preferences/grants, fixture sentinels/cache hashes and queue records; quiet, no unfinished WorkSpec). The Guardians pilot visibly scrolled existing history and retained **94/94 rendered frames**: process jank **22/94 (23.40%)**, 90 unflagged valid rows, 4 excluded flagged rows, raw `FrameCompleted − IntendedVsync` p50/p95 **19.48/34.91 ms**. The populated-detail pilot collected **before Back** and retained **44/44 frames**, split 39 + 5 across the two drawing window records, with process jank **8/44 (18.18%)**; 5 flagged rows are excluded from raw-duration statistics. Window names are identical, so their ordered raw records remain separate and are not assigned fabricated unique IDs. Evidence: `idle-frames-1788906261467543000/`, `idle-frames-1788906331844556000/`. Default-display rendering was approximately 60 Hz; no display setting changed. These are pilot results, not smoothness acceptance.

**Prospective idle batch:** five passes on existing Guardians history, each with separately reset/captured segments: one 600 ms upward swipe and 600 ms reverse swipe with observed displacement, then open the second observed terminal fixture export row and capture its populated dialog **before** Back. Preserve all frame rows and per-window statistics; check total rendered versus retained rows, exact controls, quiet queues and unchanged preferences. Back/preservation work occurs outside measured segments. Exclude both pilots; do not pool operation-loaded, startup or historical cohorts. No app restart, new task, app-filter change, output deletion, concurrent build/review agent or instrumentation is part of this batch. Accessibility-driver overhead remains a qualification. Stop on any failed control/coverage/displacement check rather than retrying until green.

The first five-pass attempt stopped during pass 1's scroll segment (`idle-frame-batch-1788906602109801000.json`), before opening detail. Controls/quiet/queue checks passed, but the helper compared **65 raw rows** against **64 rendered frames**. One row has `Flags=8` (`SkippedFrame`) and an older completion timestamp than its intended vsync; it is not a rendered frame. The raw jank observation remains **29/64 (45.31%)**. The coverage predicate now counts valid non-skipped rendered rows, separately reports skipped and invalid rows, and still requires equality with the rendered counter and zero invalid non-skipped rows. Android's [`FrameInfo.h`](https://github.com/aosp-mirror/platform_frameworks_base/blob/main/libs/hwui/FrameInfo.h) and [`JankTracker.cpp`](https://github.com/aosp-mirror/platform_frameworks_base/blob/main/libs/hwui/JankTracker.cpp) support that distinction; these are upstream references, not a claim of exact emulator-source provenance. Offline execution of the actual parser passed six assertions across all three retained dumps: each matches its rendered count, and removing one real frame makes each fail. Original evidence was not rewritten. The aborted batch remains excluded; the corrected five-pass batch is a new cohort, not an unchanged retry or a lower jank threshold.

The corrected batch also stopped in pass 1's scroll segment (`idle-frame-batch-1788906959653818000.json`; `idle-frames-1788906965679082000/`). This time the mismatch is **35 rendered versus 34 valid, unflagged retained rows**, with no skipped row to explain it. Process-reported jank is **21/35 (60.00%)**; raw-duration statistics cover only 34 rows and cannot be presented as complete coverage. All controls, unchanged-queue and quiet checks again pass. **Zero complete five-pass cohorts have been collected.** No unchanged third attempt was started. The two-agent read-only `frame-coverage-check` workflow (`wf_5825682a-a07`) completed; its full verification was read. It independently confirms the within-window gap and identifies a **plausible, not proven, reset-boundary mechanism**: current upstream HWUI can reuse a skipped current record after `RingBuffer.clear()` removes retained membership without erasing backing storage. A delayed completion for an older record across reset can produce the same 35/34 signature. The saved counters cannot distinguish these executions or establish the skipped-reason predicate/current pointer at reset, and current-main sources are not exact emulator provenance. **The second attempt remains coverage-failed; no missing-frame allowance or historical result rewrite is justified.** The bounded read-only provenance follow-up (`frame-image-provenance`, `wf_675c70cf-743`) completed with independent verification: the capture records preserve PID/boot but not an exact historical fingerprint or source revision, and no explicit provenance field was found in the 35 checked metadata records. Current local AVD/image metadata identifies `android-37.2`, Google APIs 16 KiB arm64 image revision 4, build `CP41.260814.003.B1` / incremental `16166531` (runtime SDK 37). This is **current local metadata, not historical attestation** of what PID 32688 executed. No matching AOSP manifest/tag or exact HWUI commit was established; source investigation stopped at that boundary rather than treating current-main code as proof. The separate `no-reset-frame-contract` review (`wf_0d6da905-615`) also completed, including the full adversarial result. A prospective counter delta can remove a historical offset, but equal counts alone do not establish that every newly selected row was counted in that interval: a missing counted frame plus an uncounted completed-looking row could cancel numerically. This is a contract counterexample, **not evidence that the emulator permits that ordering**. Acceptance would additionally require justified row/accounting correspondence, ordered retained-history continuity and executable same-device-clock qualification of new dialog windows. Stable dump pairs are not a substitute for those prerequisites. **The alternative remains unimplemented; complete frame coverage remains blocked.** No third batch, additional wait-until-green procedure, new tracing framework or app telemetry was added. Both reviews are now finished, and no device measurement is running. Startup cohorts, operation-loaded frames and T19 active-operation gates remain open. Neither Android app/test sources nor device settings were changed for these measurements.

A read-only retention planning check against preserved snapshot `private-stable-db-wynbmczb` also found **three of 27 sweep requests due for retention expiry within the next hour** (earliest approximately 0.31 hours at observation); the 30 DATA rows' earliest retention was approximately 15.63 hours, with no output rows. This prior stable snapshot is not a fresh startup-eligibility result, but it rules out assuming an unchanged-history one-hour cold-launch batch. No process restart, retention change or pruning was triggered to work around it. The self-grant source check and package dump show POST_NOTIFICATIONS already held; complete startup side-effect eligibility remains unestablished.

Final boundary check for this step reidentified both AVDs with exact probe-free app/test hashes, SELinux Enforcing and unchanged live PIDs (Shizuku 32688, Root 9610), quiet queues and zero READY handoffs (`queue-snapshot-1788907238948075000.json`, `queue-snapshot-1788907238449754000.json`). Shizuku headroom remains **3,600,224 KiB**; Root **7,974,716 KiB**. Three private helper syntax checks pass. Git remains on `chore/service-queue-emulator-acceptance` at `eb2345e1`, 18 tracked changed paths, clean `git diff --check`, empty staging and version 1952. No commit, push, release, grant/settings change or retained-output deletion occurred.

### 9 September probe-free Root installation and selected-cache check — passed

The separately authorized Root emulator was freshly identified as **`Thor_Root_API36` / emulator-5556, API 36, boot complete, SELinux Enforcing**, with the expected previous app `97fef0a9…` and test `958a8c93…`. A read-only quiet-boundary snapshot had no DATA tasks, two successful PRIVILEGE tasks and no READY handoffs. The guarded installer preserved both previous APKs, verified each new artifact against its own preserved bundle, exact hash, certificate, normalized manifest, app version/ABI and instrumentation runner/target, then replaced only Thor and its test APK. The Root target is now **`67f7d981…`**, test **`04bb0813…`**, the same probe-free pair as Shizuku (`probe-free-root-install-1788902143492871000/` preflight; `probe-free-root-install-1788902158399974000/record.json` installation). All three postchecks passed: controls unchanged, queue records unchanged, quiet afterward. No Home launch was hidden inside the installation; a subsequent ordinary launch was observed separately. Privilege Check displayed **KernelSU Next — Granted** and **Magisk — Granted** (`ui-1788902237878116000.json`, `ui-1788902256530890000.json`). These UI labels are not independent proof of manager implementation or physical-device behavior. No permission, broker, manager, settings or security changes were made.

For final-source functional verification, the **large fixture's original 1 MiB cache** was checked against its existing hash and metadata before the operation; its APK remains the original **128 MiB** `8b50d8ae…`. The small fixture's previously cleared cache remained confirmed absent—neither fixture was launched or reseeded. Normal Apps selection chose **only Thor QA Large**, the selected-only Cache action displayed **“clear Cache of 1 apps”**, and Confirm submitted task **`a93a9bf0-cce5-44eb-9f15-5c4c9fe695c1`**. It succeeded once, with one successful child, no degraded Root lane and released claims/session. Crucially, independent `run-as` observation afterward confirmed the formerly present large-cache file **actually missing**, rather than trusting shell exit zero or the success label. All **11 postchecks passed**, preserving PID **9610**, boot, app/grants, both original sentinels/APKs, the small fixture's existing absent-cache state, all prior/opposite queue records and WorkManager; the boundary was quiet (`root-clear-before-1788902460227856000.json`, `root-clear-after-1788902609136937000.json`, UI `ui-1788902608891770000.json`). The unchanged small control was already absent, so this run is **not a second proof that an unselected present cache survives**; that stronger control existed in the earlier small-target test. Both Root fixture caches are now intentionally absent; helpers requiring either original cache must not be rerun unchanged. No direct ADB cache deletion was used.

**Still pending:** active same-task reopening with a capture completed before terminal state; cancellation with separately accepted unfinished control work; actual DATA/PRIVILEGE overlap and bounded conflict outcomes; recovery/no-replay classification; remaining broker/lifecycle gates and T22. Read-only exploration found selection-toolbar durable Share as a lower-publication-footprint option, but it copies this monolithic fixture as APK: its duration is unmeasured and cannot inherit the nine-second XAPK window. A valid completed share can remain READY, which the existing export/installation quiet-boundary helper deliberately rejects; no share was submitted and no guard was relaxed. Root evidence is kept separate from Shizuku evidence. **T19 remains active, T22 pending; 20/22 milestones complete.**

### 8 September export lifecycle correction — emulator verification

Only the authorized `Resizable_Experimental` installation was replaced with the verified `1f5c120e…` APK using normal data-preserving installation. The preceding `3052bff1…` APK was first pulled and its hash, package/version and certificate verified in the ignored evidence directory. The pre-install snapshot was quiet; post-install granted-permission records were unchanged, and Privilege Check still displayed **Shizuku — Granted**. Root's separate APK was not replaced.

A new **prospectively excluded warmup**, `8990d151-7b52-46e2-9891-57d1c88f7697`, completed through the real export submission/navigation path with all five unique markers, single-attempt child success, and exact public APK publication/hash. After Task Close, the **first** Export reopening showed the form and `Downloads/Thor`, not the old “Exported.” banner. Four captures, with no input after opening, retained that form through **9.218 seconds**; the final screenshot independently shows it. PID **26438** remained unchanged. A subsequent quiet snapshot contained exactly six successful data tasks, proving that opening the options added no seventh task.

Evidence: `export-fix-install-1788884606989763000/`, `latency-export-1788884790997480000.json`, `export-reopen-1788884858121697000/`, and `queue-snapshot-1788884885510047000.json`, all ignored/unpublished. This accepts the reproduced completed-export reopening defect on this emulator, not physical behavior, configuration recreation or active-job return on-device. Active-job return remains JVM-covered. The first corrected-APK batch runner ended after one accepted sample without a final batch result (`latency-export-1788884940966519000.json`); this incomplete ledger is retained and not pooled or counted as a distribution. The new separately recorded batch **completed 20/20 exports, runner exit 0**, pinned to this APK/PID/boot and explicitly excluding the new warmup; earlier pilots are not pooled. Independent ledger revalidation checked 20 distinct durable IDs and 20 distinct five-event probe IDs, parent/child attempt 1, exact public bytes/hash, quiet boundaries and unchanged privilege-queue records. Both authorized emulators remained running; no Gradle/instrumentation/review agent ran alongside the batch. T19 remains active and T22 pending.

**Current-build export latency**, milliseconds from the monotonic tap marker, n=20:

| Event | Median | p95 | Maximum |
|---|---:|---:|---:|
| Durable acceptance | 2.79 | 5.66 | 32.61 |
| Logger visible | 87.06 | 104.07 | 105.23 |
| Execution admitted | 78.51 | 95.56 | 100.97 |
| First operation | 90.88 | 110.45 | 114.05 |

Median averages the middle two values; p95 uses nearest rank (19th of 20). Event latencies are not terminal/export-completion times. Raw distribution: `latency-export-1788885096223650000.json`. This is debug-APK emulator evidence with two AVDs running, not a matched-baseline comparison or speedup/regression claim.

**Selected Reinstall batch:** the first warmup preparation stopped at “Not at Apps”: Back had collapsed the expanded app-info sheet rather than dismissed it. That failed preparation is retained in `latency-privilege_sweep-1788885689600883000.json`; no Reinstall was submitted. Explicitly closing the observed sheet returned to Apps. The subsequent excluded warmup `23f698b6-6bc0-4b6c-a87b-c0aee11470ea` passed five-marker, same-PID, package-update timestamp, installer-attribution, APK-hash and data-sentinel checks. Its separate batch **completed 20/20, runner exit 0**, with no concurrent builds or other device operations. Independent ledger revalidation confirmed distinct durable/probe IDs, five markers each, parent/child attempt 1, the replacement/hash/sentinel postconditions, quiet boundaries and unchanged data-queue records. PID 26438 stayed unchanged across both batches. This selected only the small owned fixture; it was not Reinstall All.

**Current-build selected Reinstall latency**, milliseconds from tap, n=20 (same median/p95 method):

| Event | Median | p95 | Maximum |
|---|---:|---:|---:|
| Durable acceptance | 2.33 | 42.36 | 45.00 |
| Logger visible | 72.47 | 82.18 | 99.06 |
| Execution admitted | 66.21 | 94.65 | 103.15 |
| First operation | 66.24 | 94.83 | 103.64 |

Raw distribution: `latency-privilege_sweep-1788885851650984000.json`; complete runner log: `latency-runner-1788885847674298000.log`. The larger durable-acceptance tail is retained, not removed as outliers. These are stage latencies, not full reinstall duration. The same provider-provenance and unmatched-baseline limitations apply.

### 8 September Root SWEEP correction — visibility and selected-cache acceptance

A temporary opt-in `SweepRootVisibilityTest` uses the generated production Koin graph and the real SWEEP router. Its explicit isolated-runner selection suppresses normal application startup; it does not initialize WorkManager, access queue repositories, launch services, grant permissions or delete anything. The bounded command reads UID, mount namespace, allowlisted mount metadata and the two owned fixture cache files' metadata. A nonzero `stat`/`readlink` remains an unsuccessful read, not proof that a path is absent. This observes a **new instrumented process/session**, not the historical shell behind task `4974ad8e…`.

- Targeted IDE build passed with a duplicate Koin hint/D8 warning. Forced Android-test assembly also passed: **exit 0, 80/80 tasks executed**, 25s; log `root-probe-build-1788888386.log`. This is compilation, not a device pass.
- Read-only preflight reidentified `Thor_Root_API36` on `emulator-5556`, arm64, SELinux Enforcing, Thor PID 4225, and a quiet queue boundary. The installed target remains `d01194c0…`, **1952 / 1.95.2**, debuggable; its original APK and the existing test APK were preserved locally. Parsed test manifests name `ThorTestRunner` and target `com.valhalla.thor.debug`. Target, old test and new test all have the verified `df90ba95…` debug certificate; the test APK has no native ABI payload. This establishes package/signature/ABI compatibility, not every runtime linkage or whole-APK equivalence. Both original cache inodes/timestamps and data sentinels remain unchanged. Evidence: `root-probe-preflight-1788888629205275000/record.json`.
- The first preflight stopped before any install because its signer parser expected `Signer #1` while the preserved target reports `V2 Signer:`. Certificate parsing was corrected and preflight rerun; the failed attempt's preserved target remains retained. No security check was waived.
- Narrow review found one reporting defect: app-side `readlink` could throw before the shell observations were published. The test now records its errno independently. Targeted IDE validation and forced Android-test assembly passed again: **80/80 tasks, exit 0**, 25s, `root-probe-build-1788889027.log`. Direct inspection of the preserved target's `classes14.dex` confirms `onCreate` calls the overridable startup guard and returns before Koin/startup work; its manifest has no WorkManager initializer. The production router and owned executor are present in `classes5.dex`.
- **First probe executed: 1 test passed, controller exit 0.** Only test APK `4b46e7e9…` was installed; target `d01194c0…`, grants, both original fixture cache metadata/sentinels, queue rows and quiet boundaries remained unchanged. Actual SWEEP UID **0**, mode **ISOLATED**, no degraded fallback. App PID 6545 and shell PID 6622 shared mount namespace `mnt:[4026533024]`; all six fixture marker `stat` calls returned **1**. This proves the new session's unsuccessful reads despite independently present fixture files, not yet missing-path versus permission-error classification or historical-shell identity. Evidence: `root-visibility-run-1788889107496432000/record.json` and `instrumentation.txt`.
- **Second metadata probe passed, 1 test / controller exit 0:** app PID 6908 and genuine SWEEP shell PID 6964 shared `mnt:[4026533017]`; `/data/user` and `/data/user_de` were **tmpfs**, and all six marker reads were classified **not_found**, not permission-denied. The selected/control original cache files remained independently visible to their fixture processes. Target APK, grants, cache metadata/sentinels and queue rows were unchanged; both boundaries were quiet. This establishes app-data mount isolation in newly created production SWEEP sessions, while the exact historical task shell remains unattested. Forced test build: 80/80 tasks, exit 0, 22s (`root-probe-build-1788889407.log`); test APK `5cc37dd8…`; evidence `root-visibility-run-1788889451456691000/record.json`.
- **RED established before the production edit:** the factory JVM suite ran 12 tests with one intended failure (`expected 2, actual 0`); all 93 combined test/Android-test build tasks executed. Evidence: `root-visibility-red-1788889798.log` and preserved XML. The functional emulator assertion then ran **1 test / 1 intended failure**, `SWEEP must see the independently seeded user-0 fixture 0 cache`, against unchanged target `d01194c0…`; APK, grants, fixture metadata/sentinels and queue rows stayed unchanged. Evidence: `root-visibility-run-1788889879914084000/record.json`.
- The narrow production candidate now requests `FLAG_MOUNT_MASTER` for the dedicated SWEEP factory and updates its configuration comment; interactive MainShell, the separate owned executors, admission/cancellation semantics, timeout and cache deletion command are unchanged. **Host GREEN and corrected-APK visibility passed; selected-cache removal remains pending.** Focused JVM validation passed **108 tests / six suites**, zero failures/errors/skips; debug/test assembly passed in the same forced run, **110/110 tasks, exit 0**, 1m7s (`root-visibility-green-1788890113.log`). Corrected APK `97fef0a9…` was verified as 1952 / 1.95.2 with the matching certificate and compatible ABI; normalized manifests differ only by removal of Studio's `testOnly=true`. It replaced only the Root emulator target after a quiet boundary; the original APK remains preserved and grants unchanged (`root-fix-install-1788890388867892000/record.json`). The **identical functional test APK** that failed against the original target now passes **1 test / controller exit 0**: SWEEP remains UID 0 / ISOLATED / no fallback, but shell namespace `mnt:[4026532292]` differs from app `mnt:[4026533017]`, the app-data tmpfs overlays are absent from its filtered mounts, and both CE cache marker metadata match the independent original 1 MiB/inode/timestamp observations exactly. Other missing cache locations remain recorded as missing, not failures of the seeded CE assertion. APK, grants, queue rows and fixtures stayed unchanged during the probe (`root-visibility-run-1788890441309897000/record.json`). Odin may fall back from mount-master to ordinary `su`, so neither a flag assertion nor UID 0 alone closes that gate. No root-manager setting, grant, dependency, schema or version change has been made.

**Actual selected-cache operation passed on the corrected Root emulator:** after ordinary Home launch (PID **7918**), only **Thor QA Fixture** was selected and the UI confirmation explicitly named **1 app**. Quiet preflight independently rechecked both original 1 MiB cache inodes/timestamps before any submission. One confirmation created `fa17a84e-fcb6-4bbd-b97c-0fac8c314b7e`; UI showed **Succeeded / 1 of 1**, naming the selected fixture. Room has one new `CLEAR_CACHE / SUCCEEDED` parent and one successful child, both attempt 1, cleared ownership, and no degraded Root lane. The selected fixture's **own UID** now reports its marker missing; the unselected large fixture retains its original metadata **and cache SHA-256**, and both data-sentinel hashes are unchanged. PID, APK, boot identity and grants remained unchanged. Previous fixture sweep records, opposite-lane fixture rows/counts and WorkSpec records are unchanged; the final stable snapshot is quiet, schema 9, integrity/FK OK. No direct shell deletion or reseeding was used. This accepts the reproduced selected-cache failure on **this emulator**; the historical false-success task remains untouched and Odin's plain-`su` fallback still limits generalization to other Root managers.

Evidence: `root-clear-before-1788891391453723000.json`, `ui-1788891397264496000.json` (one-app confirmation), `ui-1788891415428981000.json` (terminal UI), and `root-clear-after-1788891415669463000.json`; postcondition controller **exit 0**, all 11 checks true. Private records/snapshots stay ignored and unpublished. The opt-in visibility test's seeded-marker prerequisite is no longer satisfied for the selected fixture after this intentional deletion; do not rerun it and call the expected missing marker a regression. The earlier host gates below predate the Root factory change. A fresh forced **post-Root-change full JVM run now passes: Foss and Store each 2,683 tests / 221 suites**, zero failures/errors/skips; all **88/88 tasks executed**, Gradle exit 0 in 2m22s. XML timestamps were checked against run start and each suite's actual XML preserved under `root-full-jvm-1788892014947699000/`; the recorded Corretto21 exception still applies. No operation or performance sample ran alongside that build (UI form navigation only). Post-Root lint and final probe-free rebuild/testing remain pending. T19 remains active, T22 pending, and the 20/22 milestone count is unchanged.

**Post-Root lint/removal checkpoint:** the fresh Foss lint run failed with **one `SdCardPath` error** in the temporary `SweepRootVisibilityTest` (line 114), plus 66 warnings/15 hints; all 70 tasks executed, exit 1, 2m15s. Store lint was **not run** after that failure. Evidence is retained under `root-lint-1788892981879256000/`. The diagnostic's full source was then moved to ignored `SweepRootVisibilityTest.preserved.kt`, and its sole added `ThorTestRunner` allowlist entry removed. This removes completed temporary tooling, not the persistent factory-policy regression or the Root correction; no suppression was added. Targeted IDE Android-test compilation passed afterward, retaining the known duplicate Koin hint/D8 warning. Fresh sequential lint reruns now **pass with zero errors**: Foss **66 warnings/15 hints**, 70/70 tasks, exit 0, 2m22s; Store **53 warnings/15 hints**, 51/51 tasks, exit 0, 1m57s. Both fresh XML reports and logs are preserved under `root-lint-1788893422044058000/`. This closes the temporary-diagnostic lint error without suppressing it. Latency-hook removal is now in progress, followed by fresh tests/builds; these passed lint reports predate that removal. Installed emulator APKs/test APKs have not been changed by this source cleanup.

### 8 September active-operation preparation — XAPK pilot

A prospectively excluded real `.xapk` export of the existing **134,230,491-byte large fixture APK** created `3f74eab6-80f0-4b50-acb2-8d18fcfc4e74` and succeeded, parent/child attempt 1, with settled ownership. The exact UUID-derived MediaStore output is published (`is_pending=0`) under `Download/Thor/`, **134,267,885 bytes**. ZIP integrity passed and its single APK's SHA-256 matches the installed fixture. Both fixture APKs/cache metadata/sentinels, Thor PID **26438**, grants and boot identity were unchanged; previous data tasks, opposite fixture sweep rows/counts and quiet state were checked. Evidence: `xapk-pilot-1788892522347840000/record.json`; controller exit 0, all eight checks true.

Its durable started-to-terminal interval was only **1,246 ms**. The first post-submit UI capture already showed Succeeded, so **Background was not tapped and active return/cancellation/recovery were not verified**. This pilot is not pooled into either 20-sample latency cohort. Source review confirms `.xapk` performs real copies and a no-compression ZIP; the existing cache does not enlarge exports. A proposed additive fixture-owned OBB payload was blocked at the **read-only** path check: `run-as` for the large fixture received Permission denied on `/storage/emulated/0/Android` and its OBB paths. No directory/file was created, no permission/grant/provider change or alternative-identity write was attempted. The longer active workload remains unestablished.

Remaining scenario criteria were clarified without changing their milestone: SWEEP same-package admission is bounded at **2 seconds**, then returns BUSY without mutation; successful DATA package admission has a separate **5-second** budget. Neither DATA's pre-lease EXECUTION_ADMITTED marker nor same-lane queuing proves a package-lease wait. A scoped-cancellation control must be a separately accepted task, not an already completed artifact. Export recovery may reread unfinished work under the **same UUID** with an incremented attempt; it must not be misreported as destructive-operation no-replay. No new recovery/cancellation/overlap run has occurred at this checkpoint.

### 8 September export lifecycle correction — host verification

The first-reopening defect was reproduced in a real `NavDisplay` JVM fixture using the production saveable-state/ViewModel-store decorators: **9 tests / 1 intended failure**, specifically the unexpected old “Exported.” node. Ordinary Activity-owned show/hide tests passed before the fix and are not treated as reproduction. Lifecycle 2.11's scoped-store helper preserves its store when the captured scene owner is destroyed; Nav3 can destroy that scene while retaining the underlying Apps back-stack store. The export sheet now explicitly clears **only its own scoped ViewModelStore** on composition disposal, including configuration changes. Durable submission stays process-owned; a fresh watcher rediscovers active exports. No queue, schema, gateway or permission behavior changed.

Fresh validation used `--rerun-tasks --max-workers=1` and the recorded Corretto21 exception:

- Focused GREEN: **25 tests** (10 sheet, 11 ViewModel, 4 coordinator), zero failures/errors/skips. Includes active-job reattachment and submission survival after store clearing.
- Full Foss and Store JVM suites: **2,683 tests / 221 suites each**, zero failures/errors/skips, parsed from actual JUnit XML. Debug assembly completed successfully; targeted IDE compilation also passed.
- Narrow two-file review found **no actionable blockers**. It did not run tests/device actions. The navigation fixture uses a controlled existing job and direct route push, not production-host submission or Activity recreation.
- The initial Foss lint invocation **did not produce a completion result**: output stopped at `lintAnalyzeFossDebug`; no final report or live Gradle process was found afterward. The daemon log ends without a build result; that attempt is not a pass and its termination cause is unestablished. **After both measurement batches ended, a fresh rerun completed with exit 0**, 70/70 tasks executed in 2m40s. Fresh XML confirms **zero errors, 66 `SyntheticAccessor` warnings and 15 hints**. Full output is retained as `foss-lint-1788886473.log`. The Koin/Kotlin 2.4.10 compatibility warning also remains disclosed.
- A separate fresh Store release lint run then completed with **exit 0**, 51/51 tasks executed in 1m42s. Actual XML confirms **zero errors, 53 warnings and 15 hints**; full output is retained as `store-lint-1788886938.log`. Both lint runs followed the completed measurement cohorts; neither ran alongside a measured sample.

**Corrected local APK, not yet installed at this checkpoint:** 28,416,015 bytes, SHA-256 `1f5c120ef1aa71a9e9634111252d8119d2ad9bd02869732a0bc08e5a1cdb7d3f`; independently verified package `com.valhalla.thor.debug`, version **1952 / 1.95.2**, and matching debug certificate `df90ba957f0bd9048795102bafe01745d05b9ce73d0f28d07559aaf52fd8a50b`. The Shizuku emulator still has the preceding `3052bff1…` APK. Fixed-APK export → Task Close → first Export reopening remains to be device-verified. Temporary latency probes remain. No runtime milestone, release or publication gate closes from these host results.

### 8 September real-operation checkpoint

The same `Resizable_Experimental` session was restored after the next interruption, again using the ordinary Shizuku Manager computer-start procedure and preserving grants/data. Thor and both owned fixture APK hashes survived unchanged. At this real-operation checkpoint no fresh Gradle, lint or presentation-test execution had occurred; the later export-correction host gates are recorded above. The earlier 26 instrumented passes below remain earlier evidence.

| Actual selected operation | Durable UUID | Independently observed result |
|---|---|---|
| Freeze both owned fixtures | `6f42f923-a3ae-40b4-93b3-1ea9477007d0` | UI and durable state succeeded 2/2; Android enabled state became 3 for both; installed, not hidden or suspended. |
| Unfreeze both owned fixtures | `1be6136b-a3bc-4557-989c-8d6845f28345` | UI and durable state succeeded 2/2; Android enabled state became 1; both data sentinels preserved. |
| Selected single-fixture Fix Store | `c12e6547-951a-462f-806e-58b5f499345d` | Succeeded 1/1; installer attribution changed from null to `com.android.vending`, APK bytes and data sentinel unchanged. This does not change signing identity despite the confirmation wording. |
| Single-fixture APK export | `0fcce586-66a4-4269-a5b9-7729770264fd` | Succeeded 1/1; exact UUID-derived MediaStore row published with `is_pending=0`, `Download/Thor/`, 1,061,339 readable bytes and SHA-256 identical to the installed APK. |

All four parents and every child above have attempt count 1 and cleared terminal claims/session ownership. Successful public exports have no handoff output rows; that is not evidence of a missing public file. Private snapshot acquisition checks the DB/WAL inventory and hashes before/after copying, requires two consecutive equal captures and validates schema 9, SQLite integrity and foreign keys. WorkManager storage is checked separately. These are stable quiet-boundary file captures, not an atomic cross-database SQLite backup or proof that a Worker never ran. The latest boundary has no queue services, no READY handoffs and an available empty WorkSpec table.

**Provider limitation:** shell-owned Shizuku, preserved authorization and Thor's Granted UI establish the session, not independently recorded per-task executor provenance. Task details also showed the shared Root-lane fallback warning; neither that warning nor the static Guardian roster proves which gateway executed an operation. The selected Cache action was subsequently observed absent under Shizuku, matching the Root-only guard in `MultiSelectToolBox`; no operation was forced through another entry point and no global-cache substitute counts. This verifies UI availability, not execution of the repository's unsupported-failure path.

**Timing is incomplete.** The first export UUID above is the named excluded warmup. Three later pilot exports (`20188476-c1d3-4c0b-a67d-f0f65813c5ba`, `684bbb27-417b-490c-9950-456889d7200c`, `86bdd9d9-9696-41a3-93ac-b5db667371b0`) each have a unique five-event probe, unchanged PID, terminal single-attempt success and independently checked publication/bytes. Probe UUIDs are distinct from durable UUIDs. Four navigation failures are retained across four attempt ledgers, not silently replaced. The latest failure is `Export destination/submit not visible after expansion`; subsequent inspection finds app-info rather than export options. A quiet snapshot at that point still contained exactly four successful data tasks, so those failed navigation attempts added no export task. A fifth successful export, `dc6674d8-d5e7-4859-ab9c-05df7f28c4b7`, was then run as a **prospectively excluded diagnostic**, not a timing-distribution sample; it passed the five-marker, same-PID and exact-publication checks. On its first reopening, the full UI hierarchy and intermediate screenshot show the previous **“Exported.” / “Dismiss”** result. With no drag or further input, the sheet disappeared by the next hierarchy capture, while PID 3284 remained unchanged. This is reproduced stale completion-state behavior, not merely an expansion-coordinate hypothesis. Evidence is in `latency-export-1788880235879851000.json` and `export-reopen-1788880283295687000/`; the subsequent production correction is host-verified above, not yet device-verified at that checkpoint. At that checkpoint neither 20-sample distribution existed. The later corrected-APK export distribution is recorded above; no speedup/regression verdict is justified.

Raw evidence remains ignored and unpublished under `.superpowers/sdd/2026-09-03-typed-foreground-service-queues/runtime-20260908/`, including the four latency attempt ledgers, per-action UI records and private stable snapshots. Cancellation, overlapping queues/package conflicts, Background/reopen, intentional process recovery/no replay and T22 startup/frame passes remain open. Temporary latency hooks are still present; version remains 1952.

### 8 September separately authorized Root checkpoint

The user started a second emulator and explicitly authorized Root-feature testing. Its identity is `Thor_Root_API36`, API 36, SELinux Enforcing; Thor's Privilege Check reports Root granted. Its installed 1952 APK has SHA-256 `d01194c08f35be5570da0adefdb6bccdd9cd1d682df96aafcd30f9e31b589053`, different from the known Shizuku test APK. It was **not replaced**. Subsequent read-only ZIP comparison established that **all DEX entries and all other common entries except `AndroidManifest.xml` are byte-identical** to the known Shizuku test APK; the Root APK also omits six native-library entries for other ABIs. This narrows app-bytecode provenance, but does not establish whole-APK, signature or parsed-manifest equivalence. No grant, root patch, security-setting change or action on pre-existing apps was used. The two owned fixture packages were absent, then installed from their verified local APKs and seeded; both began with their own 1 MiB cache and data sentinel. These Root observations must not be pooled into Shizuku timing or final-source acceptance.

**Cache postcondition failed:** selecting only the small fixture and confirming “clear Cache of 1 apps” created `4974ad8e-bdd5-4be5-a86f-7b687edae8cd`. UI and Room report `CLEAR_CACHE / SUCCEEDED`, 1/1, parent/child attempt 1, cleared ownership and no degraded Root lane. Independently, the selected fixture's cache file still exists at **1,048,576 bytes**. Its modification/change timestamps precede task creation, which argues against a post-clear recreation. The unselected large fixture's 1 MiB cache and both data sentinels are unchanged. Before/after snapshots pass stable-capture, schema-9, integrity/FK and quiet-boundary checks; WorkSpec is empty. The success indication is not accepted as proof of cache removal. Root cause and current-source applicability are under investigation within T19, not another milestone.

Read-only follow-up confirmed user 0 and the unchanged original cache inode/timestamps. Thor's own mountinfo shows isolated app-data mounts, consistent with a visibility hypothesis; it does **not** establish the actual SWEEP root shell's view. Namespace-link reads for the observed root shells returned exit 1 with empty output. The inspected command quoting and Odin callback path preserve the real exit status; `rm -rf` can exit 0 for an invisible/missing target, but that mechanism is not yet proven here. No deletion retry, shell-lane flag or namespace change was made.

Private evidence: `root-cache-check-4974ad8e.json`, `ui-1788879465912702000.json`, and `queue-snapshot-1788879485679673000.json` in the ignored runtime workspace. Two emulators are now running; no startup/jank or new timing distribution has been collected under this changed host load.

### 8 September restored emulator checkpoint

After the power interruption, the same user-started `Resizable_Experimental` AVD was restarted normally: API 37, boot complete, SELinux Enforcing. This is emulator evidence, not the older `Thor_Root_API36` session or a physical-device result. Shizuku was restored with its Manager-displayed computer-start command; a shell-owned `shizuku_server` was independently observed. Existing Thor broker/notification grants were preserved, and Thor's Privilege Check displayed **Shizuku — Granted**. No grant/revoke, pairing, root patch, wipe or settings workaround was used.

The source-1952 debug and Android-test builds passed (99 tasks: 6 executed, 10 from cache, 83 up-to-date; recorded Corretto21 exception, not Zulu). The installed debug APK independently matched SHA-256 `3052bff1e5bec9900873b1edab52ae70e7c2806f8b012a365ebeb0ef548b877b`, 28,416,015 bytes. Data was preserved; no pre-existing accepted work was cancelled or deleted.

Three instrumentation classes were executed separately and passed: `QueueScreenTest` **7/7**, `TaskDetailScreenTest` **18/18**, `QueueNavigationButtonTest` **1/1**. Counts come from actual instrumentation status records and final summaries, not a new JVM run. These are controlled presentation fixtures; production application startup still runs. Fake-executor integration tests and tests that grant permissions/delete shared cache were deliberately not substituted for real Shizuku acceptance.

Only newly created permission-free packages `com.valhalla.thor.acceptance.fixture` (1,061,339-byte APK) and `com.valhalla.thor.acceptance.large` (134,230,491-byte APK) are disposable targets. Installed APK hashes match their locally generated originals; each seeds its own sentinel and 1 MiB cache. Existing emulator apps are not test targets. Helpers, raw logs and private DB/WAL/SHM snapshots stay ignored under `.superpowers/`; only sanitized evidence belongs here.

At this setup checkpoint no real fixture queue operation or timing/frame batch had yet run. Per-app cache clearing is Root-only and must be tested as unsupported under Shizuku, not replaced by global cache clearing. Returning to API 37 does not establish a matched historical baseline; no speedup/regression verdict follows from it.

**The 7 September logger correction is published as `45bf8b24`; its required hosted CI passed.** [Run `34147705978`, job `101823212355`](https://github.com/trinadhthatakula/Thor/actions/runs/34147705978/job/101823212355) validated synthetic merge `617c2e6e` into `deaf370a`, with Zulu21.0.12+8 configured, Foss unit/Android-test compilation, Store lint, both release R8 tasks and the missing-rules gate. This is the preceding checkpoint, not validation of the new Recent actions or Guardians corrections. No assistant device operation was performed for these follow-ups. Reviews, fixture maintenance and validation attempts are not new milestones. No runtime milestone closes merely because host tests pass.

### 8 September primary-color publication gate

The final requested presentation adjustment uses `MaterialTheme.colorScheme.primary` for all three portraits; labels retain their existing colors. The supplied artwork is unchanged. This is a tint-only follow-up to the presentation change below, not new execution behavior or another milestone.

Final source validation used `--rerun-tasks --max-workers=1` with the recorded Corretto21 exception:

- Foss and Store each passed **2,679 tests / 221 suites**, zero failures/errors/skips, with fresh XML independently parsed. The combined test/debug-APK/Android-test compilation run executed **120/120 tasks**.
- Foss lint passed with **zero errors**, 66 warnings and 15 hints (**70/70 tasks**); Store release lint passed with **zero errors**, 53 warnings and 15 hints (**51/51 tasks**). The three supplied portrait complexity hints remain disclosed.
- A final scoped review of the **22 intended paths** found no publication blockers. It was not a whole-branch re-audit. Current 100dp worktree assets were deliberately staged instead of the older 1254dp indexed versions; their approved content hashes are unchanged. `.kotlin/`, private evidence and unrelated files are excluded.
- The targeted IDE build tool **timed out** on this final tint invocation; no successful tool result is claimed. The subsequent native Gradle gates above completed successfully.
- No new pixel-level tint/contrast or device-rendering acceptance is claimed. Existing Compose pairing, geometry, action and notification tests remain covered by the full suites; Android-test compilation is not execution.

**Current test APK:** `app/build/outputs/apk/foss/debug/app-foss-debug.apk`, **28,416,015 bytes**, package `com.valhalla.thor.debug`, version **1952 / 1.95.2**, SHA-256 `3052bff1e5bec9900873b1edab52ae70e7c2806f8b012a365ebeb0ef548b877b`. Signature verification passed with the same debug certificate `df90ba957f0bd9048795102bafe01745d05b9ce73d0f28d07559aaf52fd8a50b`. It supersedes the initial Guardians APK below.

The user authorized publication to the existing PR #453, not a merge or release. The PR remains the source for commit and hosted-check status; earlier hosted CI is not evidence for these new changes. No version bump, device operation, consent change or closure of T19/T22 runtime acceptance is part of this follow-up.

### 8 September Guardians presentation follow-up

The approved presentation change names the screen **Guardians**, retains a localized **Task queue** subtitle, and displays a static, accessible provider roster: **Groot — Root**, **Rocket — Shizuku**, **Star-Lord — Dhizuku**. The supplied 100dp vectors are used unchanged and tinted for the current theme. All eight locales define the subtitle/provider heading; character names are non-translatable proper names. The shared navigation button picks up the new accessible screen name. Technical provider settings, task statuses, the two queues and execution/storage contracts are unchanged. The roster identifies providers, not installed/authorized availability or the provider that executed a particular task; no per-task provenance is fabricated from preferences or degraded Root-shell state.

Both foreground notification builders now choose their small icon from the active task's operation: backup/restore → backup, export → download, share preparation → share, freeze → freeze, unfreeze → freeze-off, cache clearing → clear, Fix Store/reinstall → install. Preparing, claim callbacks without operation metadata and unknown operations use a neutral list icon. Character portraits never replace notification small icons. Existing notification channels, UUID intents, cancellation and active-task observation remain intact.

**Initial presentation gates (before the primary-color follow-up above):**

| Gate | Result |
|---|---|
| Regression RED | **60 tests / 12 intended failures**, zero errors/skips, exposing missing branding/locales and hardcoded icons; an earlier test-import compilation error is recorded separately, not counted as RED |
| Focused GREEN | **126 tests / 11 suites**, zero failures/errors/skips; 54/54 forced Gradle tasks executed |
| Full Foss JVM suite | **2,679 tests / 221 suites**, zero failures/errors/skips; fresh XML |
| Full Store JVM suite | **2,679 tests / 221 suites**, zero failures/errors/skips; fresh XML; combined variant run executed 88/88 tasks |
| Foss debug lint / APK / Android-test compilation | **Passed**, 104/104 tasks executed; lint zero errors, 66 `SyntheticAccessor` warnings and 15 hints |
| Store release lint | **Passed**, 51/51 tasks executed; lint zero errors, 53 `SyntheticAccessor` warnings and 15 hints |
| Targeted IDE build | **Passed**, no reported problems |
| Narrow review | **No actionable blockers** in branding, notification mapping, resources and JVM coverage; not a repeated whole-branch audit |

The roster caused three old JVM tests to expose their assumption that off-screen lazy-list rows were already composed. Tests now scroll the list to the target before existing assertions/clicks; production scrolling was not changed. Equivalent instrumented-test maintenance retains all seven tests and 28 assertion calls. Those instrumented sources **compiled but were not device-executed**. Focused coverage includes active-task icon handover, running/stopping phases, neutral fallbacks, exact character/provider pairing, 280dp RTL roster geometry at 1.5× font scale, both Recent lanes, other action dispatch and functional detail Close. Dark-theme composition is covered, not pixel-level contrast or physical rendering. Lint hints include three additional `VectorPath` complexity hints from the supplied portraits; no warnings or hints were suppressed and the assets were not rewritten. Local builds use the existing **Corretto21.0.12.1 exception**, not Zulu compliance.

**Initial Guardians test APK (superseded by the primary-tint build above):** `app/build/outputs/apk/foss/debug/app-foss-debug.apk`, **28,416,015 bytes**, package `com.valhalla.thor.debug`, version **1952 / 1.95.2**, SHA-256 `436736e33d1377c8a660c3c5271eb93120ba169fb6c7fd536491cd02d2fb665e`. Signature verification passed and certificate SHA-256 `df90ba957f0bd9048795102bafe01745d05b9ce73d0f28d07559aaf52fd8a50b` matches the previous debug APK. Packaged resources contain all three portraits and Guardians/title/subtitle/provider labels. This APK includes the inset and Recent Close corrections; the previous `c5e2ad…` logger APK does not establish these new changes.

At this initial Guardians checkpoint, all edits were **uncommitted** over `45bf8b24` and the user's existing staged/unstaged asset state was untouched. The subsequent primary-color publication gate above deliberately selects the current 100dp assets and includes all these changes. No release APK, merge, version bump, emulator operation or physical-device acceptance is claimed. T19/T22 runtime acceptance remains pending.

### 8 September Recent actions follow-up

The user reports Queue working after their own window-inset adjustment, but Recent rows still expose a Close button with no visible effect. `ACKNOWLEDGE` updates acknowledgement metadata without deleting history; the list host handles routed actions but has no detail window to dismiss. `QueueTaskRow` now omits that detail-only action and omits the action strip when no other controls remain. It preserves row selection and other advertised controls. The shared action policy, acknowledgement persistence, functional detail Close and both lines of the user's inset fix remain unchanged.

Regression evidence: before the production change, **15 tests / 2 intended failures** reproduced the Close button on Recent data and privilege rows. After the three-line list projection change, **50 tests across five Foss JVM suites passed**, zero failures/errors/skips, with all 54 Gradle tasks executed using `--rerun-tasks`. Coverage includes both Recent lanes, row selection, another action's exact UUID/dispatch without click-through, terminal detail Close and navigation dismissal. The targeted IDE build also passed with no reported problems. Local Gradle used the existing Corretto21.0.12.1 exception, not Zulu compliance. At that checkpoint, source/tests and the progress update were **uncommitted** alongside the user's preserved inset edit, with no new APK build, hosted CI, PR update or device run. The later Guardians and primary-color publication gates above include this correction and a fresh debug APK; device acceptance is still not claimed.

### 7 September logger feedback follow-up

The user reports that a bulk Fix Store request for approximately 63 apps produces approximately 64 repeated “task accepted and queued” lines, and that the footer buttons need more spacing. Tracing found one identical presentation placeholder per pending child and an existing 64-line detail cap; it did **not** prove an extra submitted task or parent acceptance line. The correction counts all pending children before applying the display cap, places one live summary after individual results, and keeps the running app separate. The count decreases when an app leaves the pending state; running is not mislabeled as queued. For oversized batches, the bounded projection must retain the current running app and recent outcomes and explicitly disclose omitted older results. Footer spacing is 16dp above the group, 8dp between controls, and an additional 6dp side inset, preserving at least 48dp controls and the non-scrolling footer. Queue execution and durable storage are outside this correction's scope. Implementation and local validation are complete; the older full-suite/CI results below remain historical checkpoints, not tests of this follow-up.

**Regression reproduction:** saved JUnit XML independently confirms an initial RED run of 14 tests / 7 failures and an expanded RED run of 24 tests / 18 failures, both with zero errors/skips. The failures expose repeated pending rows, the pre-aggregation 64-line cutoff, missing overflow disclosure and missing footer spacing. These overlapping suites are not additive totals.

**Final follow-up host gates:**

| Gate | Verified result |
|---|---|
| Full Foss JVM suite | **2,660 tests / 221 suites**, zero failures/errors/skips; fresh XML, 54/54 tasks executed |
| Full Store JVM suite | **2,660 tests / 221 suites**, zero failures/errors/skips; fresh XML, 54/54 tasks executed |
| Foss debug lint | **Zero errors**, 66 `SyntheticAccessor` warnings / 12 hints; fresh XML, 70/70 tasks executed |
| Store release lint | **Zero errors**, 53 `SyntheticAccessor` warnings / 12 hints; fresh XML, 51/51 tasks executed |
| Foss debug APK / production and Android-test compilation | **Passed**, 75/75 tasks executed; test sources compiled, not device-executed |
| Narrow review | Count/state and presentation reviews **PASS**; controller separately verified lint-maintenance delta and final 14-file patch identities |

The focused GREEN suite passes 39 tests. Production-footer fixtures measure 360×480dp and 360×400dp, including 1.3 font scale, two/three controls, 16dp top/total side inset, 8dp control gaps and at least 48dp controls. This is Robolectric evidence, not physical font/rendering confirmation. Same-length update coverage establishes final-row/footer reachability, not manually scrolled-position preservation. No shared scrolling change was made.

The first GREEN attempt exposed a real omitted-count calculation bug caused by the `buildList` receiver; the calculation was moved outside the builder without weakening assertions. The first Foss lint run found reflective resource lookup in the new test and the obsolete old queued string. Static resource IDs and removal of the obsolete key across all locales fixed those errors; `LocalePolicyTest` now requires all four replacement plurals. Both full unit suites and both lint gates were rerun after this maintenance. Initial targeted IDE compilation returned success; a later IDE wrapper timed out, while the matching Gradle daemon recorded its underlying build successful and idle before final native gates. Local launcher/daemon use the explicit **Corretto21.0.12.1 exception**, not Zulu validation.

**Corrected test APK:** `app/build/outputs/apk/foss/debug/app-foss-debug.apk`, package `com.valhalla.thor.debug`, version **1952**, **28,395,700 bytes**, SHA-256 `c5e2ad012b6c6d052d60ee88ef4911e3de91f602c18b783932fb1462998987ed`. Its verified signing certificate matches the earlier debug test APK; all four new plurals are present in packaged resources and the obsolete string is absent. Built from base `25ffb74e` plus verified implementation patch `570ec0bb8b48fb52cba16a62aa99968dabab59f9c26a0cb90ca6842a686065eb` before commit. The older APK is not the corrected build. No release APK, new device run, or new hosted-CI result is implied.

## Completed T21 review and validation

Whole-branch primary review and finite security-boundary follow-ups are complete. **Fifteen confirmed candidate IDs resolve to fourteen distinct production defects**, all implemented, focused-tested and narrowly accepted. A missed instrumentation-fixture argument was subsequently corrected and reviewed PASS/PASS without changing assertions or production APIs.

The final Foss lint gate exposed ten errors in five of the reviewed files. The bounded correction is implemented:

- Scope an API30-only test shadow accurately, retaining its existing Robolectric API36 configuration.
- Replace restricted `ViewModelStore.put` calls with public `ViewModelProvider` ownership of the same instance/key, preserving clear/cancellation/admission schedules.
- Replace six `Uri.parse` calls with equivalent `toUri()` calls, preserving the URI expressions and notification identities.

**All seven final host gates pass. Foss and Store each pass 2,643 tests across 220 suites, zero failures/errors/skips; both lint variants have zero errors.** Four APK identities/signatures, actual Koin production compilation and both executed release R8 tasks were independently checked. No `missing_rules.txt` exists. The controller independently verified all 814 input hashes, 931 evidence-file hashes and report/artifact freshness. The exact five-file lint delta passed specification/quality review: five files, 15 hunks, no unread changed hunks or blocking findings.

The APKs were built before the correction commit. Their release metadata therefore embeds `98be2f6c`; the source fingerprint below identifies their actual corrected bytes. No instrumentation execution or device acceptance is implied.

The targeted IDE tool timed out, but the matching Gradle daemon and IDE logs independently confirm that its underlying build completed successfully. That distinction is preserved: the timeout is not relabeled as a successful tool response. Native Foss reports were regenerated after that IDE build.

### Corrections — not additional milestones

| Correction group | Distinct defects | State |
|---|---:|---|
| Rollback ownership, unavailable reinstall inspection, full-UUID notification identity | 3 | Implemented, focused-tested and narrowly reviewed; included in this checkpoint |
| Restore resume, acknowledged UUID, latest-start timeout, notification retry, background key derivation | 5 | Implemented, focused-tested and narrowly reviewed; included in this checkpoint |
| SAF publication recovery and legacy-export package admission | 2 | Implemented, focused-tested and narrowly reviewed under explicit provider assumptions; included in this checkpoint |
| Profile lifetime, Queue observation failure, tile count, logger footer | 4 | Implemented, focused-tested and narrowly reviewed; corrected footer evidence accepted PASS/PASS; included in this checkpoint |

The five-file lint delta is maintenance of this correction set, not five new production defects. All other 45 correction paths remain unchanged. No lint suppression, policy/configuration/dependency change, visibility widening or new production behavior was authorized.

## Prior T21 validation evidence

Final-source fingerprint: `c10facb4a2f0c74aa2577123dee96991226f3ba01bd7f5840d6c25f35cc4d712`, covering **814 source/build inputs**, with an explicit **50-path correction inventory**. Exactly the authorized five paths differ from the preceding host snapshot.

| Check | Actual result | Scope / limitation |
|---|---|---|
| **Final-source Foss JVM suite** | **2,643 tests / 220 suites; zero failures/errors/skips** | Forced run, exit 0, 54/54 tasks executed; 06:55–06:56. Saved/native XML, identities, hashes and freshness independently verified. |
| **Final-source Foss lint** | **Exit 0; zero errors, 66 warnings, 9 hints** | Forced run, 70/70 tasks executed; 06:56–06:58. All warnings are `SyntheticAccessor`; fresh XML independently parsed. |
| **Final-source Store JVM suite** | **2,643 tests / 220 suites; zero failures/errors/skips** | Forced run, exit 0, 54/54 tasks executed; 07:09–07:10. Saved/native XML, identities, hashes and freshness independently verified. |
| **Final-source Store release lint** | **Exit 0; zero errors, 53 warnings, 9 hints** | Forced run, 51/51 tasks executed; 07:10–07:12. All warnings are `SyntheticAccessor`; fresh XML independently parsed. |
| **Final APK/Koin/R8 gates** | **Four APKs verified; all forced assemblies passed** | Debug/test: 99/99 tasks; each release: 92/92. Production Koin compilation and both R8 tasks executed, no missing-rules files; signatures, package/version identities, native/saved hashes and freshness verified. |
| Five-file lint delta review | **PASS/PASS; no blocking findings** | All five files / 15 hunks reviewed; no whole-branch re-review. |
| G6 instrumentation fixture maintenance | Compilation and targeted IDE passed; review PASS/PASS | Real package coordinator supplied, existing assertions unchanged. Compilation is not instrumented execution. |
| Web dependency correction | **304 tests / 17 files passed; audit zero vulnerabilities; typecheck/build passed** | Narrow `fast-uri` 3.1.7 lockfile update. Structural accessibility checked 11 pages; contrast excluded. |
| Correction-head hosted CI | **Required build-and-test passed** | [Run `34096882892`, job `101662471420`](https://github.com/trinadhthatakula/Thor/actions/runs/34096882892/job/101662471420), completed 08:00:49; independent live recheck 08:10:56. Actual Zulu21.0.12+8, Foss test/Android-test compilation, Store lint and both R8 tasks; missing-rules existence gate passed. Synthetic PR merge checkout; cached tasks disclosed. No hosted XML counts, instrumented execution or APK-signature claim. |
| Shizuku runtime acceptance | **Not executed for T19** | Handoff/DAO/JVM/fake tests do not establish real Shizuku operation delivery. |

### Focused review evidence

These suites overlap; their counts must not be added into a full-suite total.

| Slice | Preserved result / qualification |
|---|---|
| Ownership/reinstall/notification | RED 36 tests / 7 intended failures → GREEN 149 tests, zero failures/errors/skips; specification/quality PASS/PASS. |
| Admission/recovery/key derivation | Sixteen distinct reproduced failures → GREEN 142 tests, zero failures/errors/skips; specification/quality PASS/PASS. Android test sources compiled, not executed. |
| Export | Twelve distinct reproduced failures → GREEN 88 tests, zero failures/errors/skips. Narrow review passed with documented provider assumptions, not arbitrary-provider or power-loss durability guarantees. |
| UI/lifecycle | Initial GREEN 82 tests; corrected footer follow-up GREEN 84 tests / 9 suites, zero failures/errors/skips. Final targeted review PASS/PASS. |
| Footer evidence | Genuine mutation RED 4/4 failures. Direct logger fixtures measure 360×480dp and contain 52dp footer actions; separate real Dialog route tests verify SHARE/CANCEL/Background mapping. No claim that the separate Dialog has those dimensions. |
| Architecture-regex correction | RED 4 tests / 3 expected stack overflows → GREEN 8/8; narrow PASS/PASS. No JVM stack/configuration change. Full raw RED XML was not retained; parsed RED identities/counts and fresh GREEN evidence were preserved. |

Review coverage comprises **60 primary reports, six security cross-checks and 12 independent verification reports**, covering all **363 assigned diff segments across 359 paths**, with no missing/extra segments or primary unread ranges. The first eight reviewers failed compaction and returned no reports; none counts as coverage. Fifty-six disclosed security scopes were adjudicated into six finite boundary traces, all closed or corrected. CodeRabbit skipped the 359-file PR because its limit is 150; its SUCCESS check is not review approval.

## Completed milestones

| Task | Completed outcome |
|---|---|
| T01 | Captured the original operation-latency baseline. |
| T02 | Defined durable task contracts/state models. |
| T03 | Added the Room-backed data queue/schema. |
| T04 | Evolved privilege-sweep ownership/claim handling. |
| T05 | Implemented durable data launchers/admission. |
| T06 | Extracted archive execution into queue runners. |
| T07 | Extracted export execution into queue runners. |
| T08 | Added foreground-service platform contracts. |
| T09 | Implemented data-service execution/lifecycle/recovery. |
| T10 | Replaced the sweep store with durable queue handling. |
| T11 | Implemented/activated privilege-sweep service execution. |
| T12 | Cut migrated producers over from WorkManager; retained drain adapters. |
| T13 | Implemented durable queue/history projection (`0fc62497`). |
| T14 | Built Queue screen (`81e7e73f`). |
| T15 | Built task detail/logger UI (`baac6038`). |
| T16 | Wired durable task navigation/foreground actions (`1fe31820`). |
| T17 | Completed durable share preparation, guarded cancellation/recovery cleanup, scoped review and 45/45 focused emulator acceptance (`8efa11a7`, `ba9f8281`). |
| T18 | Completed full host validation, lint corrections, localized progress/count/Stopping notifications and scoped review (`cc67ad66`). |
| T20 | Fact-checked architecture/progress/design/plan documentation committed (`92e7d476`); PR title/body updated, feature commits pushed; branch/base/open/unmerged state verified. |
| T21 | Whole-branch review and fourteen corrections accepted; exactly 50 source/test paths plus two worker docs committed/pushed in `fd8b97c9`. All seven final local gates and required correction-head Zulu CI independently verified. |

These completed milestones do not imply T19 or T22 runtime acceptance. Single-app export and bulk share preparation are durable; multi-app “Backup”/export and single-app quick share remain intentional direct paths, as the [worker README](README.md) records.

### Historical host and emulator checkpoints

- **T18:** full Foss and Store each passed **2,566 tests / 212 suites**, zero failures/errors/skips. Foss lint: zero errors / 63 `SyntheticAccessor` warnings / nine hints; Store lint: zero errors / 52 warnings / nine hints. Four APK identities verified. These results predate T21 corrections.
- **T18 source fingerprint:** `5289a966b7d0a957fb66a6c0191b39032c4e817b66d255ddc776cf7d6c584811`. Source `cc67ad66`, intended Studio/AGP build-tool checkpoint `c8da3fb3`; APKs built before those commits and matched to source hashes.
- **T17 emulator:** 45/45 passed (13 handoff + 32 DAO), 90 matched raw start/terminal frames, no failures/errors/skips/crash markers. Approved API36 emulator, before T18; not Shizuku-operation evidence.
- **Pre-lint-correction T21:** Foss and Store each passed 2,643 tests / 220 suites at 06:36–06:39 against fingerprint `e061fa93a35c0420e4caa52c08da6e65aaf8530e6d82ec66c66a905049580c95`. Preserved separately from final-source results.

### Failures retained, not hidden by reruns

- T17 first emulator run: 43/44 passed; one invalid public-export fixture in a private-share expiry test. Fixture corrected without weakening guards; final 45/45. Two transport-blocked retries executed zero tests.
- Earlier full suite: 2,555 tests / one stale expected enum-list failure omitting intentional `BUNDLE_READ`; corrected expectation and subsequent full runs passed.
- T18 initial lint: 56 Foss / 44 Store errors, corrected without blanket suppression. Intermediate callback/projection scaffolds and an invalid oversized test fixture are distinguished from genuine production regressions. A combined multi-variant compilation OOM was recovered with serial targets, not configuration changes.
- T18 narrow review found a missing Stopping notification state. Genuine regressions, minimal correction and scoped PASS/PASS re-review closed it.
- T21 original tile-state assertion observed the wrong tile. Corrected `service.qsTile` mutation tests reproduced both failures; invalid original evidence is not counted. Original footer wrapper did not constrain the separate Dialog; the corrected evidence above closes that claim.
- Final host IDE compilation exposed the missing G6 fixture argument; the initial compiler failure remains preserved. The later lint-fix IDE tool timeout is retained separately from the proven successful underlying build.
- Final Foss lint initially failed with **10 errors: NewApi 1, RestrictedApi 3, UseKtx 6**, plus 66 warnings/nine hints. The five-file correction removes those errors without suppressing checks; fresh final-source Foss lint now passes.

## Historical pre-fixture T22 checkpoint

The distributions and navigation-only passes in this section predate the export lifecycle and Root SWEEP corrections. They remain evidence for that earlier APK/session, not final-source acceptance or samples to pool with a new cohort. Later physical-device and Root-emulator startup cohorts are recorded above; complete scrolling/populated-detail and operation-loaded frame coverage remains pending.

The bounded startup/jank brief was released only after independent T21 CI acceptance. It permits fresh approved-AVD/idleness checks, compatible replacement of the debug APK, ordinary launches/navigation and profiling—not permission/security changes or package-operation fixtures.

At 07:56:46, a stable read-only capture independently established schema9 Thor queues/items/targets and schema24 WorkManager work were all empty, with integrity checks passing and zero foreign-key violations. The initial immutable device query could not expose retained WAL-backed schema; it was not treated as evidence of empty queues or database failure. Separate WAL-aware **host copies** established the counts without device recovery/checkpointing. Private snapshots remain ignored and unpublished.

This is point-in-time idleness, not permission or a future guarantee. Fresh process/service/file-hash checks precede device writes; any change requires another consistent read-only snapshot. No accepted work is canceled/deleted to make a benchmark.

**Current-only FossDebug/API36 startup evidence is independently accepted.** The exact final debug APK was installed with ordinary `install -r`, preserving data and permissions. Twenty valid samples per condition exclude warmups and tooling failures:

| Platform metric (milliseconds) | Samples | Min | p50 | p90 | p95 | Max |
|---|---:|---:|---:|---:|---:|---:|
| Process-cold TotalTime | 20 | 1009 | 1027 | 1039 | 1041 | 1045 |
| Process-cold WaitTime | 20 | 1010 | 1028 | 1041 | 1042 | 1046 |
| Process-warm Activity recreation TotalTime | 20 | 78 | 99 | 126 | 129 | 140 |
| Process-warm Activity recreation WaitTime | 20 | 80 | 100 | 127 | 129 | 140 |

Percentiles use nearest rank. Cold means process absent, not storage-cold. Warm uses `NEW_TASK|CLEAR_TASK`, a surviving PID and a different resumed Activity—not delivery to an existing Activity. Platform timings are not fully-usable/Home-ready timings. The controller independently checked all 40 raw launch/PID/Activity records, recalculated distributions, verified 1,988 command-stream and 126 snapshot hashes, and queried 42 WAL-aware host database copies: integrity passed and accepted-work counts were zero.

The first frame pass stopped at Apps because the current hierarchy exposed no scrollable region. **Zero original full scroll/Queue passes completed.** The partial Home-to-Apps capture at approximately 60Hz contains 22 frames, native janky 4/22 and legacy janky 16/22; those counters are distinct, and the repeated window summary is not another 22 frames. This small debug-only capture, read after an idle gap, is not a regression finding. The separate navigation-only addendum then completed three **Home → Apps → Home → Queue** passes, without changing filters, settings, fixtures or accepted work:

| Navigation-only pass | Frames | Native janky | Legacy janky | Native histogram p95 |
|---|---:|---:|---:|---:|
| 1 | 13 | 6 | 10 | 46ms |
| 2 | 12 | 5 | 5 | 32ms |
| 3 | 12 | 4 | 5 | 32ms |

Each pass has explicit reset/read boundaries at approximately 60Hz; pre-reset counters were excluded. The controller verified all seven addendum-file hashes and independently parsed the saved counter outputs. Navigation/empty-Queue observations are retained as extracted UI nodes, not full new Queue XML. Empty bounded crash-buffer reads do not prove warning-free operation. These small samples, separated by UI hierarchy checks, must not be pooled with the earlier partial route or used to claim release performance.

**T22 remains partially validated, not complete:** scrolling and populated task-detail profiling are unavailable; operation-loaded and matched-baseline acceptance remain blocked. No service-queue-related regression/root cause was established, so no speculative source fix was made. Janky frames are disclosed above; this is not a claim that the UI is jank-free. Device work stopped after the three permitted passes.

## Blockers and qualifications

1. **Device/provider scope:** validation has been performed on the physical Xiaomi/POCO device and emulators. Physical-device and Root-emulator startup each completed 20 cold + 20 warm measurements; the Shizuku and Root operation results are recorded above. All three configured emulators were checked, with Shizuku and DeviceOwner readiness blockers still open. Reidentify each AVD and freshly check authorization/quiet state before further work; serial numbers are not durable identities. No broker self-grant or permission/security workaround was used. The remaining T19/T22 acceptance checks are listed above.
2. **Local JDK exception:** local evidence uses explicitly pinned Corretto21 because Zulu21 is unavailable. Required hosted run `34096882892` independently establishes Zulu21.0.12+8 validation for the correction head's synthetic PR merge; it does not relabel local results as Zulu.
3. **Baseline comparability:** the separately authorized Shizuku and Root sessions run API37 and API36 respectively; do not pool their results. A matched retained baseline APK is unproven. T22 remains limited to explicitly identified current-only debug observations, not matched speedup or shipping-release performance. Current operation-loaded frame acceptance has not yet been collected.
4. **Safe performance fixture:** queues must already be idle; never cancel/delete work or clear data to manufacture a benchmark. Warm startup needs a surviving process and genuinely recreated Activity, not an intent delivered to an existing Activity. No sampling during heavy host builds.
5. **Remaining diagnostics:** PR-preparation Foss and Store lint both passed with zero errors; Foss retains 66 warnings/15 hints, Store 53 warnings/16 hints. Earlier post-cleanup and Android Back-test validation records remain in the 9 September host/presentation section above. No warning-free, post-R8 size or performance-improvement claim. Plural adaptations are automatically checked, not native-speaker reviewed.
6. **Preserved configuration:** AGP9.5.0-alpha04, Studio's intentional Kotlin-option update, Room schema9 and version1952 remain. Baseline-profile module remains excluded. API28/29 archive package-parser runtime verification is still outstanding.
7. **Bounded guarantees:** SAF recovery assumes truthful/stable provider metadata and ordinary create/close/rename behavior. Cleanup timeouts bound cooperative suspension/lock waits, not arbitrary blocking filesystem calls; guarded retry may reclaim inaccessible files later.
8. **Shipping logging:** the bounded static check passed because shipping `DEBUG`/`PRIVILEGE_TRACE` logging is disabled, not because all Throwable forwarding is sanitized. Debug/trace builds are outside that conclusion.
9. **Emulator startup diagnosis:** the earlier emulator process ended about 1.16 seconds after a 1,000ms launch-tool timeout; a tracked background invocation survived and booted. Invocation lifetime is implicated, but the historical exit signal/sender was not captured.

## Separate completed requests

- Merged requested Dependabot/dev updates (`a3fa6321`) and preserved the intentional Android Studio/AGP update.
- Reconciled interrupted T17 edits and stopped overlapping writers; validation uses one build owner.
- Normally booted the approved API36 AVD without a wipe or security/configuration change. Serial numbers are observations, not permanent AVD identity.
- **Storage cleanup complete:** deleted exactly the two verified-unused API37 preparation images, reclaiming **4,573,888,512 allocated bytes (4.260GiB / 4.57GB)**. Installed API36 AVD/SDK, memories, transcripts/recovery inputs and other projects were preserved. The scratch API37 bundle is intentionally incomplete; no full AVD rollback backup is claimed. No further deletion or purge is planned.

## Tracking and reporting rules

The old tracker showed 38 in-progress and 72 pending labels, mostly stale or duplicate wrappers—not jobs running continuously for days. That was my tracking error. Removed 104 redundant unresolved entries without pretending their work was complete; retained historical completed records and the same 22 canonical milestones.

- This document is the human-facing source of truth; fold corrections into their existing milestone.
- Distinguish implemented, compiled, tested, reviewed, committed, pushed and merged.
- Preserve failures as history, but never present superseded results as current.
- Review small deltas narrowly; do not restart completed milestones or whole-branch review.
- PR #453 was merged by the user. Do not revive its deleted feature branch or describe it as open; remaining acceptance belongs to this local follow-up branch.

## Evidence pointers

- [Architecture](README.md), [implementation plan](../superpowers/plans/2026-09-03-typed-foreground-service-queues.md), [binding design](../superpowers/specs/2026-09-03-privilege-action-service-design.md), [latency baseline](service-queue-latency-baseline.md).
- Ignored execution workspace: `.superpowers/sdd/2026-09-03-typed-foreground-service-queues/`; its `progress.md` holds detailed handoffs and rulings.
- Current five-file maintenance: `task-21-final-lint-correction-evidence.json`, exact review diff, and preserved `task-21-final-host/09-final-foss-unit/` / `10-final-foss-lint/` reports.
- `task-21-final-host-evidence.json` records the completed seven-gate result; `task-21-final-host-controller-acceptance.json` records independent verification. Earlier failures remain preserved separately.
- `task-21-current-head-ci-evidence.json` and `task-21-current-head-ci-controller-acceptance.json` establish hosted CI separately: ten stdout and ten stderr hashes verified, actual log task/JDK/checkout provenance and fresh required-check readback. The first bounded watcher timeout and earlier canceled runs remain historical, not failures of the final required job.
- `task-22-idle-snapshot-evidence.json` and `task-22-idle-snapshot-controller-verification.json` establish capture-time idleness; `task-22-runtime-brief.md` restricts the runtime scope. No DB contents are published.
- `task-22-runtime-evidence.json` / `task-22-startup-controller-verification.json` record accepted startup and initial partial-frame evidence. `task-22-navigation-addendum.json` / `task-22-controller-disposition.json` record the three navigation-only passes and remaining blocked coverage. Raw logs, images and snapshots remain private/ignored.
- Historical T18: `task-18-host-validation/stopping-correction-evidence.json`; prior lint/resumed-host manifests remain preserved.
- Historical T17: `/tmp/thor-task17-install-9e1ac9bc/{results.json,instrumentation-events.json,instrumentation.log}`; earlier RED/transport-blocked attempts remain separately preserved.
- Native reports under `app/build/` may be regenerated; saved reports and source fingerprints identify each historical gate.
