⚡️ **Thor v1.91.2 is here!**

🧩 **Extensions store — now with update detection:**

• Installed extensions with a newer version now show a one-tap **Update** — before, it only ever said *Installed*, with no way to know an update was out
• Updates run the same **verified path** — download → SHA-256 + signature check → in-place upgrade — then the card flips back to *Installed*
• The two verified extensions got a **security + reliability update**, ready to grab: **Strombringer 1.00.1** (config store now blocks any cross-process write to the CorePatch switch) & **Thor Cluster Automator 1.00.3** (caller allowlist + safer cluster saves)

Update & enjoy! 🚀
