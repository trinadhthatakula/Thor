<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="rikka.shizuku.api" />

    <!-- Needed by both flavors: the in-app Extensions store fetches its catalog and downloads
         dedicated-key-signed extension APKs over HTTPS. Declared here (not in a flavor manifest)
         so the FOSS flavor has internet too. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.REQUEST_INSTALL_PACKAGES"
        tools:ignore="RequestInstallPackagesPolicy" />
    <!-- Needed if targeting Android 11+ for certain file types, but we use strict Stream access -->

    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="PackageVisibilityPolicy,QueryAllPackagesPermission" />

    <permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="ReservedSystemPermission" />

    <!--
      Non-AOSP. Declared by Chinese-market ROMs (MIUI/HyperOS, ColorOS, OriginOS, MagicOS)
      per T/TAF 108-2022, where package visibility is a three-state runtime permission
      (allow / while-in-use / deny) *in addition to* QUERY_ALL_PACKAGES above. On a ROM that
      grants it "while in use", getInstalledPackages() collapses once Thor is backgrounded.
      Absent on AOSP, where requesting it is a silent no-op — never assume it exists.
    -->
    <uses-permission android:name="com.android.permission.GET_INSTALLED_APPS" />

    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />

    <uses-permission android:name="shizuku.permission.API_V23" />
    <uses-permission android:name="com.rosan.dhizuku.permission.API" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <!-- Bulk-freeze results from the QS tile and launcher shortcuts, neither of which has a
         usable foreground surface: a Toast is dropped by checkCanEnqueueToast and, even when
         enqueued, draws under the shade (layer 7 vs 17). Requested normally from Settings;
         never self-granted. Inert below API 33. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- WorkManager's own manifest declares the base FOREGROUND_SERVICE permission but leaves the
         type to the app. Without the type permission AND the type attribute below, setForeground()
         throws MissingForegroundServiceTypeException on targetSdk 34+. dataSync honestly describes
         work that moves bytes; a bulk freeze sweep would need a different type and is not on this
         seam — see the spec, section 9.4. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".ThorApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/thor_drawn"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/thor_drawn_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Thor.SplashScreen"
        tools:targetApi="33">

        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.Thor.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Opening a .thorbak. The path constraint is load-bearing, not decoration:
                 PortableInstallerActivity already claims content: + application/octet-stream and
                 application/zip with no path constraint at all, which is what providers report for a
                 .thorbak — so a filter without one would put a second Thor entry in the chooser for
                 every zip and every unknown binary on the device.

                 Two layers, the same pair the installer's extension filter below uses and for the
                 same reasons:
                   (1) android:pathSuffix (API 31+) is a literal endsWith, so it matches any dot
                       count. Silently ignored on API 28-30.
                   (2) SIMPLE_GLOB pathPattern for API 28-30, whose ".*" latches onto the FIRST
                       occurrence of the next literal with NO backtracking. ".*\.thorbak" therefore
                       matches only single-dot names — and Thor's own file name is
                       "<packageName>-<versionCode>.thorbak", which always has at least two dots, so
                       that one pattern alone would match nothing Thor ever writes. Hence a ladder of
                       one pattern per dot count.

                 The ladder is lowercase-only, and that is deliberate rather than an oversight: every
                 archive extension this manifest claims (.apk, .xapk, .apks, .apkm, .apkp) gives
                 uppercase a pathSuffix and exactly one one-dot pathPattern, on the grounds that
                 nothing produces a multi-dot uppercase name. Adding five UPPERCASE rungs here would
                 make this the one block in the file that does — for a file name Thor never writes,
                 since it lowercases its own. Change all six or change none.

                 Two known limitations, both shared with that filter:
                   - Intent matching tests Uri.getPath(), so this fires for providers whose document
                     URI ends in the file name (com.android.externalstorage.documents) and not for
                     those whose URI is an opaque id (the Downloads provider).
                   - host="*" matches any NON-EMPTY authority, and a file:// URI has an empty one, so
                     in practice only content: reaches here. It is declared because lint's
                     AppLinkUrlError makes a host mandatory once any path attribute is present, and
                     file:// URIs have been unusable across app boundaries since API 24 anyway.
                 Restore from Settings is unaffected by either. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />

                <data android:pathSuffix=".thorbak" />
                <data android:pathSuffix=".THORBAK" />
                <data android:pathPattern=".*\\.thorbak" />
                <data android:pathPattern=".*\\.THORBAK" />
                <data android:pathPattern=".*\\..*\\.thorbak" />
                <data android:pathPattern=".*\\..*\\..*\\.thorbak" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.thorbak" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.thorbak" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\..*\\.thorbak" />
            </intent-filter>
        </activity>

        <service
            android:name="com.valhalla.thor.rootservice.ThorRootService"
            android:process=":root"
            android:exported="false"
            tools:ignore="Instantiatable" />

        <activity
            android:name=".presentation.installer.PortableInstallerActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/vnd.android.package-archive" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
                <data android:mimeType="application/x-apks" />
                <data android:mimeType="application/vnd.apks" />
                <data android:mimeType="application/x-xapk" />
                <data android:mimeType="application/vnd.xapk" />
                <data android:mimeType="application/x-apkm" />
                <data android:mimeType="application/apkm" />
                <data android:mimeType="application/vnd.apkm" />
                <data android:mimeType="application/vnd.apkm-archive" />
                <data android:mimeType="application/x-apkp" />
                <data android:mimeType="application/vnd.apkp" />
            </intent-filter>

            <!-- Match by extension for content/file URIs with a wildcard MIME type (common in modern file managers) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />

                <!-- Match bundle files by extension. Two layers:
                     (1) android:pathSuffix (API 31+) is a literal endsWith on the path,
                         so it matches ANY multi-dot name regardless of dot count (e.g.
                         "com.amazon.mShop.android.shopping_32.12.4.100_APKPure.xapk").
                         It is silently ignored on API <= 30.
                     (2) SIMPLE_GLOB pathPattern fallback for API 28-30: its ".*" latches
                         onto the FIRST '.' with NO backtracking, so ".*\.xapk" only
                         matches single-dot names; we enumerate one pattern per dot-count
                         (up to 6) to cover realistic version strings. pathAdvancedPattern
                         (ADVANCED_GLOB) CANNOT help — it is greedy with no backtracking,
                         so a leading ".*" eats the whole path.
                     Both matchers are case-sensitive (hence upper/lower variants); the
                     runtime type check in BundleAnalysis is case-insensitive and inspects
                     ZIP contents. -->
                <!-- .apk -->
                <data android:pathSuffix=".apk" />
                <data android:pathSuffix=".APK" />
                <data android:pathPattern=".*\\.apk" />
                <data android:pathPattern=".*\\.APK" />
                <data android:pathPattern=".*\\..*\\.apk" />
                <data android:pathPattern=".*\\..*\\..*\\.apk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.apk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.apk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\..*\\.apk" />
                <!-- .xapk -->
                <data android:pathSuffix=".xapk" />
                <data android:pathSuffix=".XAPK" />
                <data android:pathPattern=".*\\.xapk" />
                <data android:pathPattern=".*\\.XAPK" />
                <data android:pathPattern=".*\\..*\\.xapk" />
                <data android:pathPattern=".*\\..*\\..*\\.xapk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.xapk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.xapk" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\..*\\.xapk" />
                <!-- .apks -->
                <data android:pathSuffix=".apks" />
                <data android:pathSuffix=".APKS" />
                <data android:pathPattern=".*\\.apks" />
                <data android:pathPattern=".*\\.APKS" />
                <data android:pathPattern=".*\\..*\\.apks" />
                <data android:pathPattern=".*\\..*\\..*\\.apks" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.apks" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.apks" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\..*\\.apks" />
                <!-- .apkm -->
                <data android:pathSuffix=".apkm" />
                <data android:pathSuffix=".APKM" />
                <data android:pathPattern=".*\\.apkm" />
                <data android:pathPattern=".*\\.APKM" />
                <data android:pathPattern=".*\\..*\\.apkm" />
                <data android:pathPattern=".*\\..*\\..*\\.apkm" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.apkm" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.apkm" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\..*\\.apkm" />
                <!-- .apkp -->
                <data android:pathSuffix=".apkp" />
                <data android:pathSuffix=".APKP" />
                <data android:pathPattern=".*\\.apkp" />
                <data android:pathPattern=".*\\.APKP" />
                <data android:pathPattern=".*\\..*\\.apkp" />
                <data android:pathPattern=".*\\..*\\..*\\.apkp" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.apkp" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.apkp" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\..*\\.apkp" />
            </intent-filter>

        </activity>

        <!-- OFF BY DEFAULT. The last-resort filter for content:// and file:// URIs whose type
             nobody could name.

             Declaring no android:mimeType is what makes it *reach* those URIs: matchData returns
             NO_MATCH_TYPE for any intent that carries a type, and Intent.resolveTypeIfNeeded asks
             the ContentResolver first, so a content:// URI lands here only when the provider's own
             getType() returned null too. That is the GH#161 case — Samsung My Files hands over a
             MediaStore row id with no filename and no type.

             It cannot be narrowed by path. Under IntentFilter.matchData the path list is consulted
             only inside the `authorities != null` branch, so path matchers are inert without a host
             and mandatory with one: adding a host to reach the patterns would make an opaque
             provider URI unmatchable, excluding the exact case this exists to serve. There is no
             middle setting — the filter is either this broad or it is absent.

             Hence the alias, and hence android:enabled="false". Left on the activity this would put
             Thor in the "Open with" list for every file whose type nobody can name — including any
             untyped file:// URI, for which resolveType cannot consult a provider at all. (Only
             untyped: resolveType returns mType first when one is set, so a setDataAndType intent is
             rejected here like any other typed one.) docs/follow-ups/161-*.md ruled
             that trade unacceptable unenabled and prescribed exactly this shape: a separate
             component, shipped off, turned on from Settings by a user who has actually hit the bug.
             AnyFileOpenerManager flips it; PackageManager owns the state, so there is no
             preference mirroring it and nothing to drift.

             Must stay declared after its targetActivity — the platform requires the alias to follow
             the activity it points at. -->
        <activity-alias
            android:name=".presentation.installer.AnyFileInstallerAlias"
            android:targetActivity=".presentation.installer.PortableInstallerActivity"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:scheme="file" />
            </intent-filter>
        </activity-alias>


        <receiver
            android:name=".data.receivers.FreezerShortcutPinnedReceiver"
            android:exported="false" />

        <receiver
            android:name=".data.receivers.InstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.valhalla.thor.INSTALL_STATUS" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".data.receivers.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Auto Reinstall Installer of Record Sync Monitor Receiver -->
        <receiver
            android:name=".data.receivers.AutoReinstallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <service
            android:name=".presentation.tile.FreezerTileService"
            android:exported="true"
            android:icon="@drawable/frozen"
            android:label="@string/freezer"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <activity
            android:name=".presentation.launcher.FreezerLaunchActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Where a tap on a running backup/restore notification lands: it asks the UI to reopen that
             job's sheet, then resumes Thor's task.

             taskAffinity="" is load-bearing, not copied decoration. excludeFromRecents applies to the
             task this activity *roots*, so without it a tap made while no Thor task exists (the user
             exited with back, or swiped Thor away, while the foreground service kept the job running)
             roots a recents-excluded task with Thor's own affinity — and the launcher-shaped resume then
             lands HomeActivity inside that same task. Nothing re-derives the task's base intent
             afterwards, so Thor stays missing from Recents for the life of the task and is reachable
             only from the launcher icon. Its own task is throwaway; the NEW_TASK launcher intent still
             finds and resumes Thor's by affinity, which is what FreezerLaunchActivity above relies on.

             No launchMode, unlike FreezerLaunchActivity: singleInstance there keeps one tile tap from
             stacking on another, and noHistory already finishes this one inside onCreate. -->
        <activity
            android:name=".presentation.launcher.JobSheetLaunchActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Stormbringer bridge: lets the (LSPosed) launcher hook ask Thor to unsuspend a frozen
             app on launch. exported so the hook's process can reach it; access is bounded to
             Freezer packages inside the provider (the caller runs in the launcher, so it can't be
             signature-verified). -->
        <provider
            android:name=".data.provider.FreezerBridgeProvider"
            android:authorities="${applicationId}.freezerbridge"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <provider
            android:name=".data.provider.ExtensionOpsProvider"
            android:authorities="${applicationId}.extensionops"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <!-- Type overlay onto the service work-runtime declares. tools:node="merge" so this adds the
             attribute rather than replacing the library's declaration. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- koin-androidx-workmanager's AAR manifest is empty, so it does NOT remove this itself.
             workManagerFactory() below calls WorkManager.initialize() directly; leaving the startup
             initializer in place means two initializers for one WorkManager. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>